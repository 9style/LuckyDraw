# 「职场算命」计划一 · 核心引擎与主链路 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付一个可完整演示的 H5 —— 用户填 4 个字段，摇签筒，得到一份含身份标签、四维打分、吐槽、签文、彩蛋、部门梗的完整职场运势结果页。

**Architecture:** Vue 3 单页应用，无路由，由 `App.vue` 中一个 `stage` 响应式变量驱动视图切换。所有业务逻辑放在 `src/core/` 下的纯函数模块里（不依赖 Vue、不碰 DOM），可被 Vitest 完整覆盖；所有可调数据放在 `src/config/` 下的 JSON 里，零逻辑。随机源可注入种子，保证测试确定性。

**Tech Stack:** Vue 3 + Vite 5 + Vitest。生产依赖仅 `vue` 与 `qrcode-generator`（后者计划二才用）。无路由、无状态库、无 UI 组件库、无字体文件、无网络请求。

**Spec:** `docs/superpowers/specs/2026-09-13-职场算命-design.md`

## Global Constraints

以下为项目级硬约束，**每个任务的要求都隐含包含本节**：

- **零网络请求**：不得出现 `fetch` / `XMLHttpRequest` / `WebSocket` / 埋点 SDK。所有资源本地化。
- **零 `innerHTML`**：文案渲染一律走 Vue 的 `{{ }}`（自动转义）。姓名是用户输入，禁止任何形式的手动 HTML 拼接。
- **`src/core/` 不得 import Vue、不得触碰 DOM**（无 `document` / `window`）。这是单元测试可覆盖的唯一前提。例外：`storage.js` 需要 `sessionStorage`/`localStorage`，允许经由注入的 storage 对象访问，默认参数用 `globalThis.sessionStorage`。
- **`src/config/` 内不得出现任何逻辑**，只有纯数据 JSON。
- **部门大类 key 固定为 6 个**：`tech` / `product` / `market` / `ops` / `admin` / `other`。这套 key 在 `departments.json`、`scoring.json#deptBonus`、`copy/department.json`、`copy/identity.json` 四处必须齐备。
- **等级带 key 固定为 4 个**：`wei`(0-39) / `ping`(40-59) / `ji`(60-79) / `daji`(80-100)。
- **签文标签名固定为 6 个**：`上上签` / `大吉` / `中吉` / `小吉` / `平` / `小凶`。**禁止出现 `大凶`**。
- **页面内文案禁用词**：算命、占卜、改运、改命、命理、前世、因果、化解、生辰八字；以及破财、破产、失业、被裁、裁员、生病、疾病、官司、口舌、车祸、死亡、血光、离婚、分手。**例外**：`工位风水` 允许。页面展示标题只能取自 `settings.json#displayTitle`（默认「职场运势互动」），**不得硬编码「职场算命」**。
- **单条文案 ≤ 60 字**。
- **四维 key 固定为**：`moYu` / `shengZhi` / `renMai` / `caiYun`。
- **年限 key 固定为**：`lt1` / `1to3` / `3to5` / `gt5`。
- **星座 key 固定为 12 个英文名**：`aries` `taurus` `gemini` `cancer` `leo` `virgo` `libra` `scorpio` `sagittarius` `capricorn` `aquarius` `pisces`。
- Node 版本 ≥ 18（开发机为 v24）。
- **本项目不使用版本控制**：不要执行 `git init`、`git add`、`git commit` 或任何 git 命令，也不要创建 `.gitignore`。任务之间的推进靠测试通过来把关，每个任务末尾的「任务收尾」步骤只是确认测试全绿，无需产出提交。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `package.json` | 依赖与脚本（`dev` / `build` / `test` / `simulate`） |
| `vite.config.js` | Vite + Vue 插件 + Vitest 配置 |
| `index.html` | 单页宿主，含 viewport / theme-color |
| `src/main.js` | 挂载 Vue 应用 |
| `src/App.vue` | **状态机**：`stage` 驱动 `intro/form/divining/result` 四视图切换 |
| `src/styles/theme.css` | 深紫/墨蓝 + 烫金 CSS 变量与全局重置 |
| `src/styles/animation.css` | 签筒动画关键帧 |
| `src/core/config.js` | **装配**所有 `config/*.json` 为单一 `runtimeConfig`，并做一致性校验 |
| `src/core/rng.js` | 可注入种子的随机源与抽样工具 |
| `src/core/validators.js` | 4 个字段的校验与清洗 |
| `src/core/scoring.js` | 四维打分与等级带判定 |
| `src/core/copyEngine.js` | 文案插值与抽取 |
| `src/core/fortune.js` | 签文加权抽取、身份标签、组装 `reading` |
| `src/core/storage.js` | `sessionStorage` 会话读写 + `localStorage` 计数 |
| `src/core/state.js` | 全局 `reactive` store（薄封装，业务逻辑都在上面几个模块里） |
| `src/views/IntroView.vue` | 开场页 |
| `src/views/FormView.vue` | 填表页 |
| `src/views/DivinationView.vue` | 摇签筒动画 |
| `src/views/ResultView.vue` | 结果页（本计划只做完整态） |
| `src/components/ScoreRadar.vue` | 四维雷达图（页面内 SVG） |
| `src/components/ScoreBars.vue` | 四维分数条 + 等级带 |
| `src/components/FortuneSlip.vue` | 签文卡片 |
| `src/components/LuckyEggs.vue` | 幸运彩蛋 |
| `src/components/Disclaimer.vue` | 免责声明 |
| `src/config/*.json` | 纯数据配置 |
| `scripts/simulate.js` | 分数分布模拟 |
| `scripts/lint-copy.js` | 文案合规扫描（build 前置） |

**测试文件与源文件同目录**（`src/core/rng.test.js` 等），Vitest 默认 `include` 已覆盖。

---

## Task 1: 项目脚手架与构建基线

**Files:**
- Create: `package.json`, `vite.config.js`, `index.html`, `src/main.js`, `src/App.vue`, `src/styles/theme.css`

**Interfaces:**
- Consumes: 无
- Produces: 可运行的 `npm run dev` / `npm run build` / `npm run test`；`App.vue` 暴露一个根组件供后续任务填充

- [ ] **Step 1: 确认项目根目录**

本项目不使用版本控制，跳过 `git init`。确认当前目录为 `E:/Mine_Test/LuckyDraw` 即可。

- [ ] **Step 2: 创建 `package.json`**

```json
{
  "name": "lucky-draw",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "simulate": "node scripts/simulate.js"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
```

注：`build` 脚本此时只是 `vite build`。Task 14 会把它改成 `node scripts/lint-copy.js && vite build`，把合规扫描挂成构建前置。这样安排是为了让前 13 个任务的构建始终可用，而不是引用一个尚不存在的脚本。

- [ ] **Step 3: 安装依赖**

```bash
npm install
```

Expected: `added N packages`，无 error。

- [ ] **Step 4: 创建 `vite.config.js`**

```js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  base: './',
  build: {
    target: 'es2018',
    cssCodeSplit: false,
    chunkSizeWarningLimit: 800
  },
  test: {
    environment: 'node',
    // 两个目录都要收：src/ 下是纯函数层与配置层的测试，
    // scripts/ 下是构建脚本自己的测试（Task 14 的 linter 测试在那里）。
    // 漏掉 scripts/ 会让 lint-copy.test.js **永远不被收集**，
    // 而 vitest 在无匹配文件时的行为并不显眼 —— 这是本项目踩过的坑。
    include: ['src/**/*.test.js', 'scripts/**/*.test.js'],
    // Windows 上默认的 threads 池会让 vitest 自己的 %TEMP% 模块缓存出现文件占用：
    // 约 1/4 的运行只收集到部分测试文件，以 EBUSY 退出 1。
    // 已实测（Task 6 期间）：默认池 12 次中失败 2 次；本配置 10 次 0 失败。
    // 必须固定这一点 —— 测试命令给出假失败，会让真实失败被「重跑一次变绿」掩盖，
    // 而后续每个任务的验证都跑在这条命令上。
    pool: 'forks',
    poolOptions: { forks: { singleFork: true } }
  }
})
```

`base: './'` 让产物用相对路径，可直接丢进阿里云任意子目录。`cssCodeSplit: false` 把 CSS 合成一个文件，减少弱网下的请求数。

- [ ] **Step 5: 创建 `index.html`**

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover"
    />
    <meta name="theme-color" content="#1a1033" />
    <meta name="format-detection" content="telephone=no" />
    <title>职场运势互动</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

`user-scalable=no` + `maximum-scale=1` 防现场误双击缩放。标题用免责措辞（见 Global Constraints）。

- [ ] **Step 6: 创建 `src/styles/theme.css`**

```css
:root {
  --bg-deep: #14082b;
  --bg-mid: #1a1033;
  --bg-soft: #241a4a;
  --gold: #e8c074;
  --gold-bright: #f7dfa8;
  --gold-dim: #a8863f;
  --text-main: #f2ecff;
  --text-sub: #b8a9d9;
  --text-dim: #7d6fa0;
  --card-bg: rgba(255, 255, 255, 0.05);
  --card-line: rgba(232, 192, 116, 0.28);
  --radius: 16px;
  --tap: 44px;
}

* { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }

html, body {
  margin: 0;
  padding: 0;
  min-height: 100%;
  background: var(--bg-deep);
  color: var(--text-main);
  font-family: "PingFang SC", "HarmonyOS Sans", "Microsoft YaHei", system-ui, sans-serif;
  overscroll-behavior-y: none;
  /* 抑制双击缩放。viewport 里的 user-scalable=no 在 iOS Safari 上会被忽略，
     touch-action 才是跨引擎真正生效的那一个（PRD 11.2 现场防误触）。 */
  touch-action: manipulation;
}

/* 全屏容器：flex 纵向布局 + 四边安全区内边距。
   min-height 用 100dvh（动态视口高度，随移动端地址栏伸缩），100vh 作旧引擎回退。

   ⚠️ 布局约定：子元素一律用 flex:1 填充本容器，**绝不再各自声明 100vh**。
   父子都声明 100vh 时，子元素会超出父元素的 content box（父元素带安全区内边距），
   在刘海屏上凭空多出一段滚动、并让垂直居中的内容偏心。
   因为 viewport meta 带 viewport-fit=cover，env() 内边距在刘海屏上是非零的。 */
#app {
  min-height: 100vh;
  min-height: 100dvh;
  display: flex;
  flex-direction: column;
  background: radial-gradient(ellipse at 50% 0%, var(--bg-soft) 0%, var(--bg-mid) 45%, var(--bg-deep) 100%);
  padding: env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left);
}

button {
  font-family: inherit;
  font-size: 1rem;
  min-height: var(--tap);
  min-width: var(--tap);
  border: none;
  border-radius: 999px;
  cursor: pointer;
}

.btn-gold {
  background: linear-gradient(135deg, var(--gold-bright), var(--gold), var(--gold-dim));
  color: #2a1a05;
  font-weight: 700;
  letter-spacing: 0.08em;
  padding: 0 2rem;
}

.btn-gold:disabled {
  filter: grayscale(0.8) brightness(0.6);
  cursor: not-allowed;
}

.gold-text {
  background: linear-gradient(135deg, var(--gold-bright), var(--gold), var(--gold-dim));
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}
```

`--tap: 44px` 落实 PRD 11.2「点击区域 ≥ 44×44pt」。`overscroll-behavior-y: none` 抑制 iOS 橡皮筋（PRD 11.2）。

- [ ] **Step 7: 创建 `src/main.js`**

```js
import { createApp } from 'vue'
import App from './App.vue'
import './styles/theme.css'

createApp(App).mount('#app')
```

- [ ] **Step 8: 创建 `src/App.vue` 空壳**

```vue
<script setup>
import { ref } from 'vue'

const stage = ref('intro')
</script>

<template>
  <main class="app-root">
    <p>stage: {{ stage }}</p>
  </main>
</template>

<style scoped>
.app-root {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
}
</style>
```

- [ ] **Step 9: 验证构建与开发服务器**

```bash
npx vitest run --passWithNoTests
npx vite build
```

Expected: 两条命令都以退出码 0 结束。（此时还没有测试文件，必须加 `--passWithNoTests`；Task 2 起就不需要了。）

- [ ] **Step 10: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 2: 随机源 `rng.js`

**Files:**
- Create: `src/core/rng.js`
- Test: `src/core/rng.test.js`

**Interfaces:**
- Consumes: 无
- Produces:
  - `makeRng(seed: number): () => number` — 返回产生 `[0, 1)` 的函数
  - `intBetween(rng, min: number, max: number): number` — 闭区间整数
  - `pickOne(rng, arr: T[]): T | null`
  - `pickMany(rng, arr: T[], n: number): T[]` — 不重复，返回长度 `min(n, arr.length)`
  - `weightedPick(rng, items: T[], weights: number[]): T | null`

- [ ] **Step 1: 写失败测试**

```js
// src/core/rng.test.js
import { describe, it, expect } from 'vitest'
import { makeRng, intBetween, pickOne, pickMany, weightedPick } from './rng.js'

// makeRng(20260913) 的前 5 个输出。用以下命令取得后填入：
//   node -e "import('./src/core/rng.js').then(m=>{const r=m.makeRng(20260913);console.log(JSON.stringify([r(),r(),r(),r(),r()]))})"
// 注意：必须在 makeRng 的最终实现（含种子混淆）就位后再采集，否则锁的是错的算法。
const GOLDEN_SEED_20260913 = [/* 填入上面命令的输出 */]

describe('makeRng', () => {
  it('相同种子产生完全相同的序列', () => {
    const a = makeRng(12345)
    const b = makeRng(12345)
    const seqA = [a(), a(), a(), a(), a()]
    const seqB = [b(), b(), b(), b(), b()]
    expect(seqA).toEqual(seqB)
  })

  it('不同种子产生不同序列', () => {
    const a = makeRng(1)
    const b = makeRng(2)
    expect(a()).not.toBe(b())
  })

  it('输出恒在 [0, 1) 区间内', () => {
    const rng = makeRng(999)
    for (let i = 0; i < 2000; i++) {
      const v = rng()
      expect(v).toBeGreaterThanOrEqual(0)
      expect(v).toBeLessThan(1)
    }
  })

  it('种子为 0 时不退化（不会永远返回同一个值）', () => {
    const rng = makeRng(0)
    const vals = new Set([rng(), rng(), rng(), rng(), rng()])
    expect(vals.size).toBeGreaterThan(1)
  })

  it('相邻小种子不产生相关序列（种子混淆回归保护）', () => {
    // 回归保护：没有种子混淆时，makeRng(1..20) 的首次 rng() 全部落在 [0, 0.0013)，
    // buckets 会退化成只有 1 个值。这条测试就是为那个缺陷立的。
    const firsts = []
    for (let s = 1; s <= 20; s++) firsts.push(makeRng(s)())
    expect(new Set(firsts).size).toBe(20)

    const buckets = new Set()
    for (let s = 1; s <= 20; s++) buckets.add(intBetween(makeRng(s), 1, 6))
    expect(buckets.size).toBeGreaterThan(1)
  })

  it('固定种子的前 5 个输出恒定（锁死算法，防止被无意替换）', () => {
    // GOLDEN_SEED_20260913 是 makeRng(20260913) 前 5 个输出，由当前实现产出后固定下来。
    // 这条测试若失败，说明 makeRng 的算法或种子混淆被改动过 —— 那会改变全项目每一个
    // 随机结果，属于必须显式决策的变更，不是可以随手改的实现细节。
    const rng = makeRng(20260913)
    expect([rng(), rng(), rng(), rng(), rng()]).toEqual(GOLDEN_SEED_20260913)
  })
})

describe('intBetween', () => {
  it('始终落在闭区间内，且两端可达', () => {
    const rng = makeRng(42)
    const seen = new Set()
    for (let i = 0; i < 2000; i++) {
      const v = intBetween(rng, -6, 6)
      expect(Number.isInteger(v)).toBe(true)
      expect(v).toBeGreaterThanOrEqual(-6)
      expect(v).toBeLessThanOrEqual(6)
      seen.add(v)
    }
    expect(seen.has(-6)).toBe(true)
    expect(seen.has(6)).toBe(true)
  })

  it('min === max 时恒返回该值', () => {
    const rng = makeRng(7)
    expect(intBetween(rng, 5, 5)).toBe(5)
  })
})

describe('pickOne', () => {
  it('从数组中取元素', () => {
    const rng = makeRng(3)
    const arr = ['a', 'b', 'c']
    for (let i = 0; i < 50; i++) expect(arr).toContain(pickOne(rng, arr))
  })

  it('空数组返回 null', () => {
    expect(pickOne(makeRng(1), [])).toBeNull()
  })
})

describe('pickMany', () => {
  it('返回不重复的 n 个元素', () => {
    const rng = makeRng(11)
    const arr = ['a', 'b', 'c', 'd', 'e']
    const out = pickMany(rng, arr, 3)
    expect(out).toHaveLength(3)
    expect(new Set(out).size).toBe(3)
    out.forEach((x) => expect(arr).toContain(x))
  })

  it('n 超过数组长度时返回全部', () => {
    const rng = makeRng(11)
    expect(pickMany(rng, ['a', 'b'], 5)).toHaveLength(2)
  })

  it('不修改原数组', () => {
    const rng = makeRng(11)
    const arr = ['a', 'b', 'c']
    pickMany(rng, arr, 2)
    expect(arr).toEqual(['a', 'b', 'c'])
  })
})

describe('weightedPick', () => {
  it('权重为 0 的项永不被选中', () => {
    const rng = makeRng(5)
    const items = ['x', 'y']
    const weights = [0, 1]
    for (let i = 0; i < 300; i++) expect(weightedPick(rng, items, weights)).toBe('y')
  })

  it('高权重项被选中的频率显著更高', () => {
    const rng = makeRng(20260913)
    const items = ['low', 'high']
    const weights = [1, 9]
    let high = 0
    const N = 5000
    for (let i = 0; i < N; i++) if (weightedPick(rng, items, weights) === 'high') high++
    expect(high / N).toBeGreaterThan(0.85)
    expect(high / N).toBeLessThan(0.95)
  })

  it('权重全为 0 时返回 null', () => {
    expect(weightedPick(makeRng(1), ['a', 'b'], [0, 0])).toBeNull()
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npx vitest run src/core/rng.test.js
```

Expected: FAIL —— `Failed to resolve import "./rng.js"`

- [ ] **Step 3: 实现 `rng.js`**

```js
// src/core/rng.js

/**
 * xorshift32 伪随机数生成器。相同种子产生完全相同的序列，
 * 用于让「同一次占卜」的结果可复现、可测试。
 *
 * 种子先经 splitmix32 混淆再进入 xorshift。**这一步不可省略**：
 * 未混淆时相邻小整数种子的首个输出几乎线性递增（makeRng(s)() ≈ 6.295e-5 · s），
 * 实测 makeRng(1..20) 的第一次 rng() 全部落在 [0, 0.0013)，
 * 导致 intBetween 恒定返回区间下界、weightedPick 恒定选中第一项。
 * 生产环境用 Date.now() XOR 随机数（大种子）不受影响，但单元测试与
 * 任何「按计数器取种子」的调用都会踩到。
 *
 * 种子为 0 / NaN / undefined / 2^32 时统一落到 0x9e3779b9
 * （xorshift 的 0 是不动点，必须避开）。
 */
export function makeRng(seed) {
  let s = scramble((Number(seed) >>> 0) || 0x9e3779b9) || 0x9e3779b9
  return function next() {
    s ^= s << 13
    s >>>= 0
    s ^= s >>> 17
    s ^= s << 5
    s >>>= 0
    return s / 4294967296
  }
}

/** splitmix32 单轮终混：把相邻种子打散到整个 32 位空间 */
function scramble(x) {
  x = Math.imul(x ^ (x >>> 16), 0x21f0aaad)
  x = Math.imul(x ^ (x >>> 15), 0x735a2d97)
  return (x ^ (x >>> 15)) >>> 0
}

/** 闭区间 [min, max] 内的随机整数 */
export function intBetween(rng, min, max) {
  if (max <= min) return min
  return min + Math.floor(rng() * (max - min + 1))
}

export function pickOne(rng, arr) {
  if (!arr || arr.length === 0) return null
  return arr[Math.floor(rng() * arr.length)]
}

/**
 * 不重复地取 n 个（洗牌后切片），不修改原数组。
 *
 * 注意：本函数固定消耗 arr.length - 1 个随机数，与 n 无关。
 * 因此 pickMany(rng, arr, 1) 与 pickOne(rng, arr) 在同一条种子流里**不可互换**；
 * 后续任务若改动某个模块级配置数组的长度，其后的所有抽取都会整体位移。
 */
export function pickMany(rng, arr, n) {
  if (!arr || arr.length === 0) return []
  const copy = arr.slice()
  for (let i = copy.length - 1; i > 0; i--) {
    const j = Math.floor(rng() * (i + 1))
    const tmp = copy[i]
    copy[i] = copy[j]
    copy[j] = tmp
  }
  return copy.slice(0, Math.min(n, copy.length))
}

/** 按权重抽样。权重全为 0 或数组为空时返回 null */
export function weightedPick(rng, items, weights) {
  if (!items || items.length === 0) return null
  let total = 0
  for (let i = 0; i < items.length; i++) total += weights[i] || 0
  if (total <= 0) return null
  let r = rng() * total
  for (let i = 0; i < items.length; i++) {
    r -= weights[i] || 0
    if (r < 0) return items[i]
  }
  return items[items.length - 1]
}
```

`0x9e3779b9` 兜底是为了让 `seed === 0` 时不退化成恒返回 0（xorshift 的 0 是不动点）。

- [ ] **Step 4: 运行测试确认通过**

```bash
npx vitest run src/core/rng.test.js
```

Expected: PASS，全部用例通过。

- [ ] **Step 5: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 3: 字段校验 `validators.js`

**Files:**
- Create: `src/core/validators.js`
- Test: `src/core/validators.test.js`

**Interfaces:**
- Consumes: 无
- Produces:
  - `NAME_MAX = 8`
  - `validateName(raw: string): { ok: boolean, value: string, error: string | null }`
  - `validateProfile(profile, cfg): { ok: boolean, errors: Record<string, string> }`，其中 `profile = { name, dept, tenure, zodiac }`，`cfg` 提供合法枚举（`cfg.departments` 为对象、`cfg.tenures` 与 `cfg.zodiacs` 为字符串数组）

- [ ] **Step 1: 写失败测试**

```js
// src/core/validators.test.js
import { describe, it, expect } from 'vitest'
import { validateName, validateProfile, NAME_MAX } from './validators.js'

const cfg = {
  departments: { tech: { label: '技术/研发' }, other: { label: '其他' } },
  tenures: ['lt1', '1to3', '3to5', 'gt5'],
  zodiacs: ['aries', 'gemini']
}

describe('validateName', () => {
  it('拒绝空串与纯空格', () => {
    expect(validateName('').ok).toBe(false)
    expect(validateName('   ').ok).toBe(false)
    expect(validateName(null).ok).toBe(false)
    expect(validateName(undefined).ok).toBe(false)
  })

  it('接受 1 到 8 个字的中文名', () => {
    expect(validateName('张').ok).toBe(true)
    expect(validateName('张三').ok).toBe(true)
    const eight = '一二三四五六七八'
    expect([...eight]).toHaveLength(NAME_MAX)
    expect(validateName(eight).ok).toBe(true)
  })

  it('拒绝 9 个字', () => {
    const nine = '一二三四五六七八九'
    const r = validateName(nine)
    expect(r.ok).toBe(false)
    expect(r.error).toContain('8')
  })

  it('接受英文、数字与常见符号', () => {
    expect(validateName('Alice').ok).toBe(true)
    expect(validateName('李雷·Han').ok).toBe(true)
    expect(validateName('A-1_2').ok).toBe(true)
  })

  it('拒绝 XSS 注入载荷', () => {
    expect(validateName('<script>alert(1)</script>').ok).toBe(false)
    expect(validateName('<img src=x onerror=alert(1)>').ok).toBe(false)
    expect(validateName('"><svg/onload=1>').ok).toBe(false)
    expect(validateName('张三&李四').ok).toBe(false)
  })

  it('拒绝所有 HTML 相关短字符（必须由正则拦截，而非长度）', () => {
    // ⚠️ 上面那组载荷（25 / 27 / 16 个码点）全部超过 NAME_MAX=8，
    // 在长度分支就被拦下了，**根本没走到正则**。实测把 NAME_RE 放宽到
    // 允许 < > " ' & 后，那组测试依然全绿 —— 即安全测试没测到安全机制本身。
    // 下面这组载荷都 ≤ 8 个码点，必然走到正则，专门补这个洞。
    const dangerous = [
      'a<b', 'a>b', 'a"b', "a'b", 'a&b', 'a/b', 'a=b', 'a`b',
      'a＜b', // 全角小于号 U+FF1C，NFKC 归一化会变成 <
      'a﹤b', // small less-than U+FE64
      'a〈b'  // CJK 左尖括号 U+3008
    ]
    for (const payload of dangerous) {
      expect([...payload].length).toBeLessThanOrEqual(NAME_MAX) // 前提：确实能走到正则
      expect(validateName(payload).ok).toBe(false)
    }
  })

  it('接受 CJK 扩展 A 汉字（生僻字姓名不被误拒）', () => {
    expect(validateName('䶮').ok).toBe(true) // U+4DAE，扩展 A，真实姓名用字
  })

  it('拒绝 emoji', () => {
    expect(validateName('张三😀').ok).toBe(false)
  })

  it('去除首尾空格后返回清洗值', () => {
    const r = validateName('  张三  ')
    expect(r.ok).toBe(true)
    expect(r.value).toBe('张三')
  })
})

describe('validateProfile', () => {
  const good = { name: '张三', dept: 'tech', tenure: '1to3', zodiac: 'aries' }

  it('四项齐备时通过', () => {
    expect(validateProfile(good, cfg).ok).toBe(true)
  })

  it('缺少部门时报错并给出该字段的错误', () => {
    const r = validateProfile({ ...good, dept: '' }, cfg)
    expect(r.ok).toBe(false)
    expect(r.errors.dept).toBeTruthy()
  })

  it('部门不在枚举内时报错', () => {
    const r = validateProfile({ ...good, dept: 'not_a_dept' }, cfg)
    expect(r.ok).toBe(false)
    expect(r.errors.dept).toBeTruthy()
  })

  it('年限不在枚举内时报错', () => {
    const r = validateProfile({ ...good, tenure: 'lt0' }, cfg)
    expect(r.ok).toBe(false)
    expect(r.errors.tenure).toBeTruthy()
  })

  it('星座不在枚举内时报错', () => {
    const r = validateProfile({ ...good, zodiac: 'ophiuchus' }, cfg)
    expect(r.ok).toBe(false)
    expect(r.errors.zodiac).toBeTruthy()
  })

  it('姓名非法时错误挂在 name 上', () => {
    const r = validateProfile({ ...good, name: '<b>x</b>' }, cfg)
    expect(r.ok).toBe(false)
    expect(r.errors.name).toBeTruthy()
  })

  it('全部非法时四项都有错误', () => {
    const r = validateProfile({ name: '', dept: 'x', tenure: 'x', zodiac: 'x' }, cfg)
    expect(r.ok).toBe(false)
    expect(Object.keys(r.errors).sort()).toEqual(['dept', 'name', 'tenure', 'zodiac'])
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npx vitest run src/core/validators.test.js
```

Expected: FAIL —— `Failed to resolve import "./validators.js"`

- [ ] **Step 3: 实现 `validators.js`**

```js
// src/core/validators.js

export const NAME_MAX = 8

// 中文、英文字母、数字，以及常见姓名符号 · - _ . 与空格。
//
// 汉字范围用「CJK 扩展 A（U+3400–U+4DBF）+ 基本区全段（U+4E00–U+9FFF）」，
// 而不是 Unicode 1.1 时代的 U+4E00–U+9FA5 —— 后者会拒掉扩展 A 的汉字
// （如 䶮 U+4DAE，真实姓名用字），现场遇到生僻字姓名会直接卡死，
// 而用户屏幕上明明显示的是汉字，只会觉得产品坏了。
const NAME_RE = /^[\u3400-\u4dbf\u4e00-\u9fffA-Za-z0-9·\-_. ]+$/

export function validateName(raw) {
  const value = String(raw ?? '').trim()
  if (value.length === 0) {
    return { ok: false, value, error: '请填写名字' }
  }
  if ([...value].length > NAME_MAX) {
    return { ok: false, value, error: `名字最多 ${NAME_MAX} 个字` }
  }
  if (!NAME_RE.test(value)) {
    return { ok: false, value, error: '名字只能用中文、字母、数字和 · - _ .' }
  }
  return { ok: true, value, error: null }
}

export function validateProfile(profile, cfg) {
  const errors = {}

  const nameResult = validateName(profile?.name)
  if (!nameResult.ok) errors.name = nameResult.error

  const dept = profile?.dept
  if (!dept || !Object.prototype.hasOwnProperty.call(cfg.departments, dept)) {
    errors.dept = '请选择部门'
  }

  if (!cfg.tenures.includes(profile?.tenure)) {
    errors.tenure = '请选择入职年限'
  }

  if (!cfg.zodiacs.includes(profile?.zodiac)) {
    errors.zodiac = '请选择星座'
  }

  return { ok: Object.keys(errors).length === 0, errors }
}
```

白名单正则（而非黑名单）是这里的关键 —— 它天然拒绝 `<` `>` `&` `"` `'` 以及 emoji，不依赖任何转义逻辑。

- [ ] **Step 4: 运行测试确认通过**

```bash
npx vitest run src/core/validators.test.js
```

Expected: PASS

- [ ] **Step 5: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 4: 配置文件与装配层 `config.js`

**Files:**
- Create: `src/config/settings.json`, `src/config/departments.json`, `src/config/scoring.json`, `src/core/config.js`
- Test: `src/core/config.test.js`

**Interfaces:**
- Consumes: `validators.js` 的枚举需求
- Produces: `runtimeConfig` 对象，结构为
  ```
  {
    settings: { passcode, displayTitle, donationText, eventName, organizer, posterBaseUrl, enableCount },
    departments: { tech: { label, jokeKey }, ... },
    scoring: { dimensions, dimensionLabels, zodiacBase, tenureBonus, deptBonus, jitter, bands },
    tenures: ['lt1','1to3','3to5','gt5'],
    tenureLabels: { lt1: '应届萌新', ... },
    zodiacs: ['aries', ...12],
    zodiacLabels: { aries: '白羊座', ... }
  }
  ```

- [ ] **Step 1: 创建 `src/config/settings.json`**

```json
{
  "passcode": "搬砖",
  "displayTitle": "职场运势互动",
  "donationText": "【待填写：本次筹款将捐至 XX 公益项目】",
  "eventName": "【待填写：活动名称】",
  "organizer": "【待填写：主办方】",
  "posterBaseUrl": "",
  "enableCount": true
}
```

- [ ] **Step 2: 创建 `src/config/departments.json`**

```json
{
  "tech":    { "label": "技术 / 研发",   "jokeKey": "tech" },
  "product": { "label": "产品 / 设计",   "jokeKey": "product" },
  "market":  { "label": "市场 / 销售",   "jokeKey": "market" },
  "ops":     { "label": "运营 / 客服",   "jokeKey": "ops" },
  "admin":   { "label": "人力 / 行政 / 财务", "jokeKey": "admin" },
  "other":   { "label": "其他（自填）",  "jokeKey": "other" }
}
```

- [ ] **Step 3: 创建 `src/config/scoring.json`**

12 星座键为 `aries` `taurus` `gemini` `cancer` `leo` `virgo` `libra` `scorpio` `sagittarius` `capricorn` `aquarius` `pisces`。**每维数值须覆盖 45~90 的跨度，且每个星座的四维均分互相接近（极差 ≤ 5）**。

```json
{
  "dimensions": ["moYu", "shengZhi", "renMai", "caiYun"],
  "dimensionLabels": {
    "moYu": "摸鱼指数",
    "shengZhi": "升职运",
    "renMai": "人脉运",
    "caiYun": "财运"
  },
  "zodiacBase": {
    "aries":       { "moYu": 45, "shengZhi": 90, "renMai": 70, "caiYun": 75 },
    "taurus":      { "moYu": 90, "shengZhi": 45, "renMai": 75, "caiYun": 70 },
    "gemini":      { "moYu": 70, "shengZhi": 75, "renMai": 45, "caiYun": 90 },
    "cancer":      { "moYu": 75, "shengZhi": 70, "renMai": 90, "caiYun": 45 },
    "leo":         { "moYu": 55, "shengZhi": 80, "renMai": 70, "caiYun": 75 },
    "virgo":       { "moYu": 80, "shengZhi": 55, "renMai": 75, "caiYun": 70 },
    "libra":       { "moYu": 70, "shengZhi": 75, "renMai": 55, "caiYun": 80 },
    "scorpio":     { "moYu": 75, "shengZhi": 70, "renMai": 80, "caiYun": 55 },
    "sagittarius": { "moYu": 65, "shengZhi": 70, "renMai": 75, "caiYun": 70 },
    "capricorn":   { "moYu": 70, "shengZhi": 65, "renMai": 70, "caiYun": 75 },
    "aquarius":    { "moYu": 75, "shengZhi": 70, "renMai": 65, "caiYun": 70 },
    "pisces":      { "moYu": 70, "shengZhi": 75, "renMai": 70, "caiYun": 65 }
  },
  "tenureBonus": {
    "lt1":  { "moYu": -6, "shengZhi": 8,  "renMai": 2,  "caiYun": -4 },
    "1to3": { "moYu": -1, "shengZhi": 3,  "renMai": 3,  "caiYun": 1 },
    "3to5": { "moYu": 3,  "shengZhi": -2, "renMai": 5,  "caiYun": 3 },
    "gt5":  { "moYu": 8,  "shengZhi": -4, "renMai": 6,  "caiYun": 5 }
  },
  "deptBonus": {
    "tech":    { "moYu": 6,  "shengZhi": -2, "renMai": -4, "caiYun": 2 },
    "product": { "moYu": 2,  "shengZhi": 3,  "renMai": 4,  "caiYun": -3 },
    "market":  { "moYu": -5, "shengZhi": 5,  "renMai": 8,  "caiYun": 5 },
    "ops":     { "moYu": -3, "shengZhi": 2,  "renMai": 6,  "caiYun": -1 },
    "admin":   { "moYu": 4,  "shengZhi": -4, "renMai": 5,  "caiYun": 3 },
    "other":   { "moYu": 0,  "shengZhi": 0,  "renMai": 0,  "caiYun": 0 }
  },
  "jitter": 6,
  "bands": [
    { "max": 39,  "key": "wei",  "label": "危",  "emoji": "💀" },
    { "max": 59,  "key": "ping", "label": "平",  "emoji": "😐" },
    { "max": 79,  "key": "ji",   "label": "吉",  "emoji": "👍" },
    { "max": 100, "key": "daji", "label": "大吉", "emoji": "🌟" }
  ]
}
```

星座数值是按「每维覆盖 45~90、每星座四维均分落在 66~70」设计的，满足 spec 6.3 的两条约束。

- [ ] **Step 4: 写失败测试**

```js
// src/core/config.test.js
import { describe, it, expect } from 'vitest'
import { runtimeConfig, checkConfigConsistency, isPlaceholder } from './config.js'

const DEPT_KEYS = ['tech', 'product', 'market', 'ops', 'admin', 'other']
const DIMS = ['moYu', 'shengZhi', 'renMai', 'caiYun']

describe('runtimeConfig 装配', () => {
  it('暴露 settings 全部字段', () => {
    const s = runtimeConfig.settings
    expect(s.passcode).toBeTruthy()
    expect(s.displayTitle).toBeTruthy()
    expect(typeof s.donationText).toBe('string')
    expect(typeof s.posterBaseUrl).toBe('string')
    expect(typeof s.enableCount).toBe('boolean')
  })

  it('展示标题不含迷信措辞', () => {
    const banned = ['算命', '占卜', '改运', '改命', '命理', '前世', '因果', '化解', '生辰八字']
    banned.forEach((w) => expect(runtimeConfig.settings.displayTitle).not.toContain(w))
  })

  it('部门为 6 个大类', () => {
    expect(Object.keys(runtimeConfig.departments).sort()).toEqual([...DEPT_KEYS].sort())
  })

  it('年限与星座枚举齐备', () => {
    expect(runtimeConfig.tenures).toEqual(['lt1', '1to3', '3to5', 'gt5'])
    expect(runtimeConfig.zodiacs).toHaveLength(12)
    runtimeConfig.zodiacs.forEach((z) => expect(runtimeConfig.zodiacLabels[z]).toBeTruthy())
  })
})

describe('scoring.json 约束', () => {
  const { zodiacBase, deptBonus, bands, dimensions } = runtimeConfig.scoring

  it('四维 key 一致', () => {
    expect(dimensions).toEqual(DIMS)
  })

  it('每维数值跨度覆盖 45~90', () => {
    for (const dim of DIMS) {
      const vals = Object.values(zodiacBase).map((z) => z[dim])
      expect(Math.min(...vals)).toBeLessThanOrEqual(45)
      expect(Math.max(...vals)).toBeGreaterThanOrEqual(90)
    }
  })

  it('每个星座的四维均分极差 ≤ 5（不得贬低任一星座）', () => {
    const avgs = Object.entries(zodiacBase).map(([z, v]) => {
      const avg = DIMS.reduce((s, d) => s + v[d], 0) / DIMS.length
      return { zodiac: z, avg }
    })
    const values = avgs.map((a) => a.avg)
    expect(Math.max(...values) - Math.min(...values)).toBeLessThanOrEqual(5)
  })

  it('deptBonus 覆盖全部 6 个大类且四维齐备', () => {
    expect(Object.keys(deptBonus).sort()).toEqual([...DEPT_KEYS].sort())
    for (const k of DEPT_KEYS) {
      for (const dim of DIMS) expect(typeof deptBonus[k][dim]).toBe('number')
    }
  })

  it('等级带为四档且上界覆盖到 100', () => {
    expect(bands.map((b) => b.key)).toEqual(['wei', 'ping', 'ji', 'daji'])
    expect(Math.max(...bands.map((b) => b.max))).toBe(100)
  })
})

describe('checkConfigConsistency', () => {
  it('当前配置无缺失，返回空数组', () => {
    expect(checkConfigConsistency()).toEqual([])
  })
})

describe('isPlaceholder', () => {
  it('识别未填写的占位符', () => {
    expect(isPlaceholder('【待填写：主办方】')).toBe(true)
    expect(isPlaceholder('【待填写】')).toBe(true)
  })

  it('空值视为未填写', () => {
    expect(isPlaceholder('')).toBe(true)
    expect(isPlaceholder(null)).toBe(true)
    expect(isPlaceholder(undefined)).toBe(true)
  })

  it('真实值不误判', () => {
    expect(isPlaceholder('XX 公司工会')).toBe(false)
    expect(isPlaceholder('本次筹款将捐至儿童助学项目')).toBe(false)
    // 以书名号开头但并非占位符的正常文案不得被误伤
    expect(isPlaceholder('【官方】公益市集')).toBe(false)
  })
})
```

- [ ] **Step 5: 运行测试确认失败**

```bash
npx vitest run src/core/config.test.js
```

Expected: FAIL —— `Failed to resolve import "./config.js"`

- [ ] **Step 6: 实现 `src/core/config.js`**

```js
// src/core/config.js
import settings from '../config/settings.json'
import departments from '../config/departments.json'
import scoring from '../config/scoring.json'

export const TENURES = ['lt1', '1to3', '3to5', 'gt5']

export const TENURE_LABELS = {
  lt1: '入职不到 1 年',
  '1to3': '入职 1-3 年',
  '3to5': '入职 3-5 年',
  gt5: '入职 5 年以上'
}

export const ZODIACS = [
  'aries', 'taurus', 'gemini', 'cancer', 'leo', 'virgo',
  'libra', 'scorpio', 'sagittarius', 'capricorn', 'aquarius', 'pisces'
]

export const ZODIAC_LABELS = {
  aries: '白羊座', taurus: '金牛座', gemini: '双子座', cancer: '巨蟹座',
  leo: '狮子座', virgo: '处女座', libra: '天秤座', scorpio: '天蝎座',
  sagittarius: '射手座', capricorn: '摩羯座', aquarius: '水瓶座', pisces: '双鱼座'
}

export const runtimeConfig = {
  settings,
  departments,
  scoring,
  tenures: TENURES,
  tenureLabels: TENURE_LABELS,
  zodiacs: ZODIACS,
  zodiacLabels: ZODIAC_LABELS
}

/**
 * 校验 6 个部门大类在四处配置中齐备。
 * 返回缺失项描述数组，空数组表示一致。启动时调用，缺失只 warn 不抛。
 */
export function checkConfigConsistency() {
  const problems = []
  const deptKeys = Object.keys(departments)
  const bonusKeys = Object.keys(scoring.deptBonus || {})

  for (const k of deptKeys) {
    if (!bonusKeys.includes(k)) problems.push(`scoring.deptBonus 缺少部门 "${k}"`)
  }
  for (const k of bonusKeys) {
    if (!deptKeys.includes(k)) problems.push(`departments.json 缺少部门 "${k}"`)
  }
  return problems
}

/**
 * 判断一个 settings 值是否仍是未填写的占位符。
 *
 * 用途：占位符是给运营看的内部批注（如 `【待填写：主办方】`），
 * **绝不能渲染到页面上** —— 在公益活动上显示「待填写：本次筹款将捐至 XX 公益项目」，
 * 比不显示这一行更糟。
 *
 * 之所以用 `startsWith('【待填写')` 而不是只判 `【`：后者会把任何以书名号
 * 开头的正常文案（如 `【官方】`）误判为占位符 —— 那是**假阳性**。
 * 代价是留下**假阴性**：手写变体（`[待填写：X]`、裸 `待填写：X`、`【待定】`）
 * 识不出来，仍会渲染到页面上。
 *
 * 这个残留缺口**故意**不在守卫里补：守卫要精确，不能误伤正常文案；而
 * Task 18 的上线闸门要保守（宁可误报），用更宽的模式兜底。两者不一致是设计，
 * 不是疏漏 —— 不要为了「统一」把守卫放宽。
 */
export function isPlaceholder(value) {
  return !value || String(value).startsWith('【待填写')
}
```

`copy/department.json` 与 `copy/identity.json` 的同类校验在 Task 12、14 追加到本函数。

- [ ] **Step 7: 运行测试确认通过**

```bash
npx vitest run src/core/config.test.js
```

Expected: PASS。若「四维均分极差 ≤ 5」失败，微调 `zodiacBase` 中偏差星座的数值至满足。

- [ ] **Step 8: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 5: 存储 `storage.js`

**Files:**
- Create: `src/core/storage.js`
- Test: `src/core/storage.test.js`

**Interfaces:**
- Consumes: 无
- Produces:
  - `saveSession(data, store?)` / `loadSession(store?)` / `clearSession(store?)`
  - `bumpCount(store?)` / `getCount(store?)` / `resetCount(store?)`
  - `SESSION_KEY = 'ld_session'`，`COUNT_KEY = 'ld_count'`
  - 所有函数末位参数为可注入的 storage 对象（默认 `globalThis.sessionStorage` / `globalThis.localStorage`），使 `core/` 保持无 DOM 依赖、可测

- [ ] **Step 1: 写失败测试**

```js
// src/core/storage.test.js
import { describe, it, expect, beforeEach } from 'vitest'
import {
  saveSession, loadSession, clearSession,
  bumpCount, getCount, resetCount,
  SESSION_KEY, COUNT_KEY
} from './storage.js'

function makeMemoryStore() {
  const map = new Map()
  return {
    getItem: (k) => (map.has(k) ? map.get(k) : null),
    setItem: (k, v) => map.set(k, String(v)),
    removeItem: (k) => map.delete(k),
    _dump: () => Object.fromEntries(map)
  }
}

let session
let local

beforeEach(() => {
  session = makeMemoryStore()
  local = makeMemoryStore()
})

describe('会话读写', () => {
  it('保存后能原样读回', () => {
    const data = {
      profile: { name: '张三', dept: 'tech', tenure: '1to3', zodiac: 'aries' },
      reading: { scores: { moYu: 72, shengZhi: 55, renMai: 81, caiYun: 63 } },
      unlocked: false,
      stage: 'result'
    }
    saveSession(data, session)
    expect(loadSession(session)).toEqual(data)
  })

  it('无数据时返回 null', () => {
    expect(loadSession(session)).toBeNull()
  })

  it('数据损坏时返回 null 而不抛异常', () => {
    session.setItem(SESSION_KEY, '{ 这不是合法 json')
    expect(loadSession(session)).toBeNull()
  })

  it('storage 不可用时静默降级，不抛异常', () => {
    const broken = {
      getItem() { throw new Error('QuotaExceededError') },
      setItem() { throw new Error('QuotaExceededError') },
      removeItem() { throw new Error('QuotaExceededError') }
    }
    expect(() => saveSession({ a: 1 }, broken)).not.toThrow()
    expect(loadSession(broken)).toBeNull()
    expect(() => clearSession(broken)).not.toThrow()
  })

  it('clearSession 后读不到数据', () => {
    saveSession({ a: 1 }, session)
    clearSession(session)
    expect(loadSession(session)).toBeNull()
  })
})

describe('计数', () => {
  it('初始为 0', () => {
    expect(getCount(local)).toBe(0)
  })

  it('每次 bump 递增 1', () => {
    bumpCount(local)
    bumpCount(local)
    expect(getCount(local)).toBe(3 - 1)
  })

  it('计数存的是数字不是字符串', () => {
    bumpCount(local)
    expect(typeof JSON.parse(local.getItem(COUNT_KEY))).toBe('number')
  })

  it('值损坏时归零而不是 NaN', () => {
    local.setItem(COUNT_KEY, 'abc')
    expect(getCount(local)).toBe(0)
  })

  it('resetCount 归零', () => {
    bumpCount(local)
    resetCount(local)
    expect(getCount(local)).toBe(0)
  })

  it('计数读写不抛异常即使 storage 不可用', () => {
    const broken = { getItem() { throw new Error('x') }, setItem() { throw new Error('x') }, removeItem() {} }
    expect(() => bumpCount(broken)).not.toThrow()
    expect(getCount(broken)).toBe(0)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npx vitest run src/core/storage.test.js
```

Expected: FAIL —— `Failed to resolve import "./storage.js"`

- [ ] **Step 3: 实现 `storage.js`**

```js
// src/core/storage.js

export const SESSION_KEY = 'ld_session'
export const COUNT_KEY = 'ld_count'

function defaultSession() {
  try {
    return globalThis.sessionStorage
  } catch {
    return null
  }
}

function defaultLocal() {
  try {
    return globalThis.localStorage
  } catch {
    return null
  }
}

/** 读写一律吞异常：隐私模式、配额超限、storage 被禁用都不能打断主流程 */
export function saveSession(data, store = defaultSession()) {
  if (!store) return
  try {
    store.setItem(SESSION_KEY, JSON.stringify(data))
  } catch {
    /* 静默降级：会话恢复是增强功能，失败不影响主流程 */
  }
}

export function loadSession(store = defaultSession()) {
  if (!store) return null
  try {
    const raw = store.getItem(SESSION_KEY)
    if (!raw) return null
    const parsed = JSON.parse(raw)
    if (!parsed || typeof parsed !== 'object') return null
    return parsed
  } catch {
    return null
  }
}

export function clearSession(store = defaultSession()) {
  if (!store) return
  try {
    store.removeItem(SESSION_KEY)
  } catch {
    /* 同上 */
  }
}

export function getCount(store = defaultLocal()) {
  if (!store) return 0
  try {
    const n = Number(JSON.parse(store.getItem(COUNT_KEY)))
    return Number.isFinite(n) && n >= 0 ? n : 0
  } catch {
    return 0
  }
}

export function bumpCount(store = defaultLocal()) {
  const next = getCount(store) + 1
  if (!store) return next
  try {
    store.setItem(COUNT_KEY, JSON.stringify(next))
  } catch {
    /* 计数失败无所谓 */
  }
  return next
}

export function resetCount(store = defaultLocal()) {
  if (!store) return
  try {
    store.setItem(COUNT_KEY, JSON.stringify(0))
  } catch {
    /* 同上 */
  }
}
```

所有 storage 访问都包在 try/catch 里 —— iOS 无痕模式下 `localStorage.setItem` 会直接抛异常，未捕获会打断整个流程。

- [ ] **Step 4: 运行测试确认通过**

```bash
npx vitest run src/core/storage.test.js
```

Expected: PASS

- [ ] **Step 5: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 6: 全局状态与状态机

**Files:**
- Create: `src/core/state.js`
- Modify: `src/App.vue`

**Interfaces:**
- Consumes: `storage.js` 的 `saveSession` / `loadSession` / `clearSession`
- Produces:
  - `store` —— `reactive` 对象：`{ stage, profile, reading, unlocked }`
  - `setStage(stage)`、`setProfile(profile)`、`setReading(reading)`、`unlock()`、`resetAll()`
  - `restore()` —— 启动时从 session 恢复，返回是否恢复成功

- [ ] **Step 1: 实现 `src/core/state.js`**

```js
// src/core/state.js
import { reactive } from 'vue'
import { saveSession, loadSession, clearSession } from './storage.js'

export const STAGES = ['intro', 'form', 'divining', 'result']

export const store = reactive({
  stage: 'intro',
  profile: null,
  reading: null,
  unlocked: false
})

function persist() {
  saveSession({
    stage: store.stage,
    profile: store.profile,
    reading: store.reading,
    unlocked: store.unlocked
  })
}

export function setStage(stage) {
  if (!STAGES.includes(stage)) return
  store.stage = stage
  persist()
}

export function setProfile(profile) {
  store.profile = profile
  persist()
}

export function setReading(reading) {
  store.reading = reading
  persist()
}

export function unlock() {
  store.unlocked = true
  persist()
}

export function resetAll() {
  store.stage = 'intro'
  store.profile = null
  store.reading = null
  store.unlocked = false
  clearSession()
}

/**
 * 从 sessionStorage 恢复。只有 profile 与 reading 都在时才恢复到结果页，
 * 否则回到开场页 —— 避免恢复到一半的空状态。
 */
export function restore() {
  const saved = loadSession()
  if (!saved || !saved.profile || !saved.reading) return false
  store.profile = saved.profile
  store.reading = saved.reading
  store.unlocked = Boolean(saved.unlocked)
  store.stage = saved.stage === 'result' || saved.stage === 'divining' ? 'result' : 'form'
  return true
}
```

只有 `profile` 与 `reading` 同时存在才恢复 —— 恢复到一半的空状态比不恢复更糟。

- [ ] **Step 2: 改写 `src/App.vue` 为状态机**

```vue
<script setup>
import { onMounted } from 'vue'
import { store, setStage, restore } from './core/state.js'
import { checkConfigConsistency } from './core/config.js'
import IntroView from './views/IntroView.vue'
import FormView from './views/FormView.vue'
import DivinationView from './views/DivinationView.vue'
import ResultView from './views/ResultView.vue'

onMounted(() => {
  checkConfigConsistency().forEach((p) => console.warn('[config]', p))
  restore()
})
</script>

<template>
  <div class="app-root">
    <Transition name="stage-fade" mode="out-in">
      <IntroView v-if="store.stage === 'intro'" key="intro" @start="setStage('form')" />
      <FormView v-else-if="store.stage === 'form'" key="form" @submitted="setStage('divining')" />
      <DivinationView v-else-if="store.stage === 'divining'" key="divining" @done="setStage('result')" />
      <ResultView v-else key="result" />
    </Transition>
  </div>
</template>

<style scoped>
.app-root {
  /* 用 flex:1 填充 #app 的 content box，不声明 min-height:100vh ——
     见 theme.css 里 #app 的布局约定说明。 */
  flex: 1;
  display: flex;
  flex-direction: column;
  padding: 0 20px;
}
</style>

<style>
.stage-fade-enter-active,
.stage-fade-leave-active {
  transition: opacity 0.32s ease;
}
.stage-fade-enter-from,
.stage-fade-leave-to {
  opacity: 0;
}
</style>
```

过渡样式必须放在非 scoped 的 `<style>` 块里 —— scoped 会给选择器加属性选择器，`Transition` 的类名是加在子组件根元素上的，scoped 版本匹配不到。

- [ ] **Step 3: 创建四个视图占位文件**

`src/views/IntroView.vue`：
```vue
<script setup>
defineEmits(['start'])
</script>

<template>
  <section><button class="btn-gold" @click="$emit('start')">开始</button></section>
</template>
```

`src/views/FormView.vue`：
```vue
<script setup>
defineEmits(['submitted'])
</script>

<template>
  <section><button class="btn-gold" @click="$emit('submitted')">提交</button></section>
</template>
```

`src/views/DivinationView.vue`：
```vue
<script setup>
import { onMounted } from 'vue'
const emit = defineEmits(['done'])
onMounted(() => setTimeout(() => emit('done'), 300))
</script>

<template>
  <section>摇签中…</section>
</template>
```

`src/views/ResultView.vue`：
```vue
<template>
  <section>结果页</section>
</template>
```

这四个文件在 Task 7、8、14、16 会被完整替换。此处只用于验证状态机能跑通。

- [ ] **Step 4: 验证状态机**

```bash
npx vite build
```

Expected: 构建成功，无 error。

```bash
npm run dev
```

手动验证：浏览器打开后点「开始」→ 进入表单 → 点「提交」→ 300ms 后进入结果页。四个阶段切换正常。

- [ ] **Step 5: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 7: 开场页与免责声明

**Files:**
- Create: `src/components/Disclaimer.vue`
- Modify: `src/views/IntroView.vue`

**Interfaces:**
- Consumes: `runtimeConfig.settings` 的 `displayTitle` / `donationText` / `organizer`
- Produces: `<Disclaimer />` 组件，无 props，自带 `#disclaimer` 埋点供测试定位

- [ ] **Step 1: 创建 `src/components/Disclaimer.vue`**

```vue
<script setup>
const TEXT = '本页面为纯娱乐互动，结果随机生成，不构成任何建议。所有信息仅在本地处理，不采集、不上传。'
</script>

<template>
  <p id="disclaimer" class="disclaimer">{{ TEXT }}</p>
</template>

<style scoped>
.disclaimer {
  margin: 0;
  font-size: 12px;
  line-height: 1.6;
  color: var(--text-dim);
  text-align: center;
}
</style>
```

文案逐字取自 PRD 第八章第 3 条，是全项目合规的硬要求，**不得改写**。

- [ ] **Step 2: 实现 `src/views/IntroView.vue`**

```vue
<script setup>
import { runtimeConfig, isPlaceholder } from '../core/config.js'
import Disclaimer from '../components/Disclaimer.vue'

defineEmits(['start'])
const { displayTitle, donationText, organizer } = runtimeConfig.settings

// 占位符是给运营看的内部批注，绝不能渲染给参会者。
// 两个字段用同一个判定，不给「一个守卫另一个不守卫」留任何不对称。
const showDonation = !isPlaceholder(donationText)
const showOrganizer = !isPlaceholder(organizer)
</script>

<template>
  <section class="intro">
    <div class="intro__mark">✦</div>
    <h1 class="intro__title gold-text">{{ displayTitle }}</h1>
    <p class="intro__sub">打工人能量图鉴</p>

    <div class="intro__card">
      <p>填 4 个空，摇一支签</p>
      <p>看看今天的你，是哪种职场生物</p>
    </div>

    <div v-if="showDonation || showOrganizer" class="intro__donation">
      <p v-if="showDonation" class="intro__donation-main">{{ donationText }}</p>
      <p v-if="showOrganizer" class="intro__donation-sub">主办：{{ organizer }}</p>
    </div>

    <button class="btn-gold intro__btn" @click="$emit('start')">开 始</button>

    <Disclaimer />
  </section>
</template>

<style scoped>
.intro {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 14px;
  padding: 32px 0 24px;
  text-align: center;
}
.intro__mark { font-size: 40px; color: var(--gold); }
.intro__title { margin: 0; font-size: 34px; letter-spacing: 0.16em; }
.intro__sub { margin: 0; color: var(--text-sub); letter-spacing: 0.3em; font-size: 13px; }
.intro__card {
  margin-top: 22px;
  padding: 20px 26px;
  border: 1px solid var(--card-line);
  border-radius: var(--radius);
  background: var(--card-bg);
  color: var(--text-sub);
  line-height: 1.9;
  font-size: 15px;
}
.intro__card p { margin: 0; }
.intro__donation { margin-top: 8px; }
.intro__donation-main { margin: 0; color: var(--gold); font-size: 14px; }
.intro__donation-sub { margin: 4px 0 0; color: var(--text-dim); font-size: 12px; }
.intro__btn { margin-top: 18px; }
</style>
```

- [ ] **Step 3: 验证渲染**

```bash
npm run dev
```

手动验证：
1. 开场页显示「职场运势互动」标题（**不是「职场算命」**）
2. 页面底部可见免责声明全文
3. 捐款公示文案可见
4. 打开 DevTools Console 执行 `document.body.innerText.includes('算命')` → 应为 `false`

- [ ] **Step 4: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 8: 填表页

**Files:**
- Modify: `src/views/FormView.vue`

**Interfaces:**
- Consumes: `runtimeConfig`（departments / tenures / tenureLabels / zodiacs / zodiacLabels）、`validators.js` 的 `validateProfile`、`state.js` 的 `setProfile`
- Produces: `FormView` emit `submitted`，并在 emit 前调用 `setProfile(cleaned)`

- [ ] **Step 1: 实现 `src/views/FormView.vue`**

```vue
<script setup>
import { ref, computed } from 'vue'
import { runtimeConfig } from '../core/config.js'
import { validateProfile } from '../core/validators.js'
import { setProfile } from '../core/state.js'

const emit = defineEmits(['submitted'])

const deptList = Object.entries(runtimeConfig.departments).map(([key, v]) => ({
  key,
  label: v.label
}))

const form = ref({
  name: '',
  dept: '',
  tenure: '',
  zodiac: ''
})

const touched = ref({})
const submitted = ref(false)

const validation = computed(() => validateProfile(form.value, runtimeConfig))
const errors = computed(() => (submitted.value || Object.keys(touched.value).length ? validation.value.errors : {}))

function blur(field) {
  touched.value = { ...touched.value, [field]: true }
}

function onSubmit() {
  submitted.value = true
  if (!validation.value.ok) return
  setProfile({
    name: form.value.name.trim(),
    dept: form.value.dept,
    tenure: form.value.tenure,
    zodiac: form.value.zodiac
  })
  emit('submitted')
}
</script>

<template>
  <section class="form">
    <h2 class="form__title gold-text">报上名来</h2>

    <div class="field">
      <label for="f-name">名字</label>
      <input
        id="f-name"
        v-model="form.name"
        type="text"
        maxlength="8"
        placeholder="怎么称呼你"
        autocomplete="off"
        @blur="blur('name')"
        @keyup.enter="onSubmit"
      />
      <p v-if="errors.name" class="field__err">{{ errors.name }}</p>
    </div>

    <div class="field">
      <label for="f-dept">部门</label>
      <select id="f-dept" v-model="form.dept" @blur="blur('dept')" @change="blur('dept')">
        <option value="" disabled>选一个</option>
        <option v-for="d in deptList" :key="d.key" :value="d.key">{{ d.label }}</option>
      </select>
      <p v-if="errors.dept" class="field__err">{{ errors.dept }}</p>
    </div>

    <div class="field">
      <label>入职多久了</label>
      <div class="chips">
        <button
          v-for="t in runtimeConfig.tenures"
          :key="t"
          type="button"
          class="chip"
          :class="{ 'chip--on': form.tenure === t }"
          @click="form.tenure = t; blur('tenure')"
        >
          {{ runtimeConfig.tenureLabels[t] }}
        </button>
      </div>
      <p v-if="errors.tenure" class="field__err">{{ errors.tenure }}</p>
    </div>

    <div class="field">
      <label for="f-zodiac">星座</label>
      <select id="f-zodiac" v-model="form.zodiac" @blur="blur('zodiac')" @change="blur('zodiac')">
        <option value="" disabled>选一个</option>
        <option v-for="z in runtimeConfig.zodiacs" :key="z" :value="z">
          {{ runtimeConfig.zodiacLabels[z] }}
        </option>
      </select>
      <p v-if="errors.zodiac" class="field__err">{{ errors.zodiac }}</p>
    </div>

    <button class="btn-gold form__btn" :disabled="!validation.ok" @click="onSubmit">开 始 摇 签</button>
    <p class="form__hint">信息只在你的手机上计算，不会上传</p>
  </section>
</template>

<style scoped>
.form {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 18px;
  padding: 28px 0;
}
.form__title { margin: 0 0 6px; font-size: 26px; letter-spacing: 0.12em; text-align: center; }
.field { display: flex; flex-direction: column; gap: 8px; }
.field label { font-size: 14px; color: var(--text-sub); }
.field input,
.field select {
  min-height: var(--tap);
  padding: 0 14px;
  border: 1px solid var(--card-line);
  border-radius: 12px;
  background: var(--card-bg);
  color: var(--text-main);
  font-size: 16px;
  font-family: inherit;
}
.field input:focus,
.field select:focus { outline: 2px solid var(--gold-dim); outline-offset: 1px; }
.field__err { margin: 0; font-size: 12px; color: #ff9a9a; }
.chips { display: flex; flex-wrap: wrap; gap: 8px; }
.chip {
  padding: 0 14px;
  border: 1px solid var(--card-line);
  background: var(--card-bg);
  color: var(--text-sub);
  font-size: 13px;
  border-radius: 999px;
}
.chip--on { background: var(--gold); color: #2a1a05; border-color: var(--gold); font-weight: 700; }
.form__btn { margin-top: 8px; }
.form__hint { margin: 0; text-align: center; font-size: 12px; color: var(--text-dim); }
</style>
```

表单**一页填完不分页**（PRD 第四章体验要求）；名字输入框支持回车提交（PRD 11.2）；按钮在未通过校验时置灰（PRD 第四章"不合法不允许点开始"）。

- [ ] **Step 2: 验证**

```bash
npm run dev
```

手动验证：
1. 四字段任一为空时「开始摇签」按钮为置灰不可点
2. 名字输入 `<script>` → 失焦后显示错误提示，按钮保持置灰
3. 名字输入 9 个字 → 被 `maxlength` 截断为 8
4. 填完四项 → 按钮恢复可点
5. 名字输入「张三」回车 → 进入动画阶段

- [ ] **Step 3: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 9: 打分引擎 `scoring.js`

**Files:**
- Create: `src/core/scoring.js`
- Test: `src/core/scoring.test.js`

**Interfaces:**
- Consumes: `rng.js` 的 `intBetween`；`config.js` 的 `runtimeConfig`
- Produces:
  - `bandOf(score: number, bands): string` — 返回 `wei|ping|ji|daji`
  - `computeScores(profile, scoringCfg, rng): { scores: Record<dim, number>, bands: Record<dim, string> }`
  - `averageScore(scores, dimensions): number`

- [ ] **Step 1: 写失败测试**

```js
// src/core/scoring.test.js
import { describe, it, expect } from 'vitest'
import { bandOf, computeScores, averageScore } from './scoring.js'
import { makeRng } from './rng.js'
import { runtimeConfig } from './config.js'

const cfg = runtimeConfig.scoring
const DIMS = cfg.dimensions
const BANDS = cfg.bands

const profile = { name: '张三', dept: 'tech', tenure: '1to3', zodiac: 'gemini' }

describe('bandOf', () => {
  it('边界值判定正确', () => {
    expect(bandOf(0, BANDS)).toBe('wei')
    expect(bandOf(39, BANDS)).toBe('wei')
    expect(bandOf(40, BANDS)).toBe('ping')
    expect(bandOf(59, BANDS)).toBe('ping')
    expect(bandOf(60, BANDS)).toBe('ji')
    expect(bandOf(79, BANDS)).toBe('ji')
    expect(bandOf(80, BANDS)).toBe('daji')
    expect(bandOf(100, BANDS)).toBe('daji')
  })
})

describe('computeScores', () => {
  it('返回全部四个维度', () => {
    const { scores, bands } = computeScores(profile, cfg, makeRng(1))
    expect(Object.keys(scores).sort()).toEqual([...DIMS].sort())
    expect(Object.keys(bands).sort()).toEqual([...DIMS].sort())
  })

  it('分数恒在 0~100 且为整数', () => {
    const profiles = []
    for (const z of runtimeConfig.zodiacs) {
      for (const t of runtimeConfig.tenures) {
        for (const d of Object.keys(runtimeConfig.departments)) {
          profiles.push({ ...profile, zodiac: z, tenure: t, dept: d })
        }
      }
    }
    for (const p of profiles) {
      const { scores } = computeScores(p, cfg, makeRng(7))
      for (const dim of DIMS) {
        expect(Number.isInteger(scores[dim])).toBe(true)
        expect(scores[dim]).toBeGreaterThanOrEqual(0)
        expect(scores[dim]).toBeLessThanOrEqual(100)
      }
    }
  })

  it('等级带与分数一一对应', () => {
    const { scores, bands } = computeScores(profile, cfg, makeRng(3))
    for (const dim of DIMS) expect(bands[dim]).toBe(bandOf(scores[dim], BANDS))
  })

  it('相同种子结果完全一致', () => {
    const a = computeScores(profile, cfg, makeRng(20260913))
    const b = computeScores(profile, cfg, makeRng(20260913))
    expect(a).toEqual(b)
  })

  it('不同种子结果不同（随机波动生效）', () => {
    const a = computeScores(profile, cfg, makeRng(1))
    const b = computeScores(profile, cfg, makeRng(2))
    expect(a.scores).not.toEqual(b.scores)
  })
})

describe('分数分布', () => {
  it('10000 次抽样中四个等级带都出现过（危档可达回归保护）', () => {
    const rng = makeRng(20260913)
    const zodiacs = runtimeConfig.zodiacs
    const tenures = runtimeConfig.tenures
    const depts = Object.keys(runtimeConfig.departments)
    const seen = new Set()
    for (let i = 0; i < 10000; i++) {
      const p = {
        ...profile,
        zodiac: zodiacs[Math.floor(rng() * zodiacs.length)],
        tenure: tenures[Math.floor(rng() * tenures.length)],
        dept: depts[Math.floor(rng() * depts.length)]
      }
      const { bands } = computeScores(p, cfg, rng)
      for (const dim of DIMS) seen.add(bands[dim])
    }
    expect([...seen].sort()).toEqual(['daji', 'ji', 'ping', 'wei'])
  })
})

describe('averageScore', () => {
  it('返回四维算术平均', () => {
    expect(averageScore({ moYu: 80, shengZhi: 60, renMai: 70, caiYun: 90 }, DIMS)).toBe(75)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npx vitest run src/core/scoring.test.js
```

Expected: FAIL —— `Failed to resolve import "./scoring.js"`

- [ ] **Step 3: 实现 `scoring.js`**

```js
// src/core/scoring.js
import { intBetween } from './rng.js'

/** 分数落到哪个等级带。bands 按 max 升序排列，末档兜底。 */
export function bandOf(score, bands) {
  for (const b of bands) {
    if (score <= b.max) return b.key
  }
  return bands[bands.length - 1].key
}

export function clamp(v, min, max) {
  return Math.max(min, Math.min(max, v))
}

/**
 * 四维打分：星座基础值 + 年限加成 + 部门加成 + 随机波动。
 * rng 决定波动，同一种子结果完全可复现。
 */
export function computeScores(profile, scoringCfg, rng) {
  const { dimensions, zodiacBase, tenureBonus, deptBonus, jitter, bands } = scoringCfg
  const base = zodiacBase[profile.zodiac]
  const tenure = tenureBonus[profile.tenure]
  const dept = deptBonus[profile.dept]

  const scores = {}
  const resultBands = {}

  for (const dim of dimensions) {
    const noise = intBetween(rng, -jitter, jitter)
    const raw = (base?.[dim] ?? 60) + (tenure?.[dim] ?? 0) + (dept?.[dim] ?? 0) + noise
    const score = clamp(Math.round(raw), 0, 100)
    scores[dim] = score
    resultBands[dim] = bandOf(score, bands)
  }

  return { scores, bands: resultBands }
}

export function averageScore(scores, dimensions) {
  if (!dimensions.length) return 0
  const total = dimensions.reduce((sum, d) => sum + (scores[d] ?? 0), 0)
  return total / dimensions.length
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
npx vitest run src/core/scoring.test.js
```

Expected: PASS

- [ ] **Step 5: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 10: 分布模拟脚本

**Files:**
- Create: `scripts/simulate.js`

**Interfaces:**
- Consumes: `src/core/scoring.js`、`src/core/config.js`、`src/core/rng.js`
- Produces: 命令行可执行的分布报告

- [ ] **Step 1: 实现 `scripts/simulate.js`**

```js
// scripts/simulate.js
// 用法：node scripts/simulate.js [抽样次数]
// 作用：打印四维分数的等级带分布，供调整 scoring.json 权重时参考。
//
// ⚠️ 这里**直接导入 JSON**（带 import attribute），而不是 `import { runtimeConfig } from '../src/core/config.js'`。
// 原因：Node 的 ESM 要求 JSON 导入必须带 `with { type: 'json' }`，而 `config.js`
// 的裸 JSON 导入是给 Vite 用的 —— 在纯 Node 下会抛 ERR_IMPORT_ATTRIBUTE_MISSING
// （已实测确认）。本脚本是独立的开发脚本、不进 Vite 产物，所以用 attribute 形式
// 既安全又无需为它改动 `config.js`（改了反而要冒构建工具兼容性的风险）。
import scoring from '../src/config/scoring.json' with { type: 'json' }
import departments from '../src/config/departments.json' with { type: 'json' }
import { computeScores } from '../src/core/scoring.js'
import { makeRng } from '../src/core/rng.js'

const N = Number(process.argv[2]) || 10000
const cfg = scoring
const DIMS = cfg.dimensions
const zodiacs = Object.keys(cfg.zodiacBase)
const tenures = Object.keys(cfg.tenureBonus)
const depts = Object.keys(departments)
const rng = makeRng(20260913)

const bandCount = {}
const scoreSum = {}
const scoreMin = {}
const scoreMax = {}
for (const dim of DIMS) {
  bandCount[dim] = { wei: 0, ping: 0, ji: 0, daji: 0 }
  scoreSum[dim] = 0
  scoreMin[dim] = Infinity
  scoreMax[dim] = -Infinity
}

for (let i = 0; i < N; i++) {
  const profile = {
    name: 'x',
    zodiac: zodiacs[Math.floor(rng() * zodiacs.length)],
    tenure: tenures[Math.floor(rng() * tenures.length)],
    dept: depts[Math.floor(rng() * depts.length)]
  }
  const { scores, bands } = computeScores(profile, cfg, rng)
  for (const dim of DIMS) {
    bandCount[dim][bands[dim]] += 1
    scoreSum[dim] += scores[dim]
    scoreMin[dim] = Math.min(scoreMin[dim], scores[dim])
    scoreMax[dim] = Math.max(scoreMax[dim], scores[dim])
  }
}

const pad = (s, n) => String(s).padEnd(n, ' ')
// 分母必须显式传入：分维度行分母是抽样次数 N，
// 而「整体」行是四维计数之和，分母为 N * DIMS.length。
// 早先这里把两者混用，导致整体行打印出 216.1% 这种数字。
const pct = (n, denom = N) => ((n / denom) * 100).toFixed(1) + '%'

console.log(`\n抽样 ${N} 次（seed=20260913）\n`)
console.log(pad('维度', 12) + pad('均值', 8) + pad('区间', 14) + pad('危', 8) + pad('平', 8) + pad('吉', 8) + '大吉')
console.log('-'.repeat(66))
for (const dim of DIMS) {
  const label = cfg.dimensionLabels[dim]
  const b = bandCount[dim]
  console.log(
    pad(label, 12) +
      pad((scoreSum[dim] / N).toFixed(1), 8) +
      pad(`${scoreMin[dim]}~${scoreMax[dim]}`, 14) +
      pad(pct(b.wei), 8) +
      pad(pct(b.ping), 8) +
      pad(pct(b.ji), 8) +
      pct(b.daji)
  )
}

const allBands = { wei: 0, ping: 0, ji: 0, daji: 0 }
for (const dim of DIMS) for (const k of Object.keys(allBands)) allBands[k] += bandCount[dim][k]
const total = N * DIMS.length
console.log('-'.repeat(66))
console.log(
  pad('整体', 12) + pad('', 8) + pad('', 14) +
    pad(pct(allBands.wei, total), 8) + pad(pct(allBands.ping, total), 8) +
    pad(pct(allBands.ji, total), 8) + pct(allBands.daji, total)
)

const missing = Object.entries(allBands).filter(([, v]) => v === 0).map(([k]) => k)
if (missing.length) {
  console.log(`\n⚠️  以下等级带在 ${N} 次抽样中从未出现：${missing.join(', ')}`)
  console.log('   这些档位的文案永远不会被用户看到，请调整 scoring.json。')
} else {
  console.log('\n✅ 四个等级带均可达。')
}
console.log('')
```

- [ ] **Step 2: 运行脚本**

```bash
npm run simulate
```

Expected: 打印四维分布表，末行为 `✅ 四个等级带均可达。`。若某档缺失，按提示微调 `scoring.json`（放宽 `jitter` 或加大 `zodiacBase` 跨度）后重跑。

- [ ] **Step 3: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 11: 文案引擎 `copyEngine.js`

**Files:**
- Create: `src/core/copyEngine.js`
- Test: `src/core/copyEngine.test.js`

**Interfaces:**
- Consumes: `rng.js` 的 `pickMany`、`pickOne`
- Produces:
  - `interpolate(text: string, vars: object): string` — 替换 `{key}`，未知 key 原样保留
  - `pickText(pool: string[], rng, n = 1): string[]` — 不重复取 n 条
  - `pickEntry(pool: object[], rng): object | null`

- [ ] **Step 1: 写失败测试**

```js
// src/core/copyEngine.test.js
import { describe, it, expect } from 'vitest'
import { interpolate, pickText, pickEntry } from './copyEngine.js'
import { makeRng } from './rng.js'

describe('interpolate', () => {
  it('替换已知变量', () => {
    expect(interpolate('{name} 今天宜早退', { name: '张三' })).toBe('张三 今天宜早退')
  })

  it('替换多个变量', () => {
    expect(interpolate('{name} 在 {dept}', { name: '张三', dept: '技术/研发' }))
      .toBe('张三 在 技术/研发')
  })

  it('未知变量原样保留', () => {
    expect(interpolate('{name} 的 {unknown}', { name: '张三' })).toBe('张三 的 {unknown}')
  })

  it('同一变量出现多次全部替换', () => {
    expect(interpolate('{name}{name}', { name: 'A' })).toBe('AA')
  })

  it('变量值为 0 或空串时正常替换', () => {
    expect(interpolate('{n}', { n: 0 })).toBe('0')
    expect(interpolate('{n}', { n: '' })).toBe('')
  })

  it('不解析 HTML —— 尖括号原样输出', () => {
    expect(interpolate('{name}', { name: '<b>x</b>' })).toBe('<b>x</b>')
  })

  it('空文本返回空串', () => {
    expect(interpolate('', {})).toBe('')
  })
})

describe('pickText', () => {
  const pool = ['a', 'b', 'c', 'd', 'e']

  it('取 n 条且互不重复', () => {
    const out = pickText(pool, makeRng(1), 3)
    expect(out).toHaveLength(3)
    expect(new Set(out).size).toBe(3)
    out.forEach((x) => expect(pool).toContain(x))
  })

  it('n 大于池子大小时返回全部且不重复', () => {
    const out = pickText(['a', 'b'], makeRng(1), 5)
    expect(out.sort()).toEqual(['a', 'b'])
  })

  it('空池返回空数组', () => {
    expect(pickText([], makeRng(1), 2)).toEqual([])
    expect(pickText(null, makeRng(1), 2)).toEqual([])
  })

  it('n 为 0 时返回空数组', () => {
    expect(pickText(pool, makeRng(1), 0)).toEqual([])
  })
})

describe('pickEntry', () => {
  it('返回池中一个对象', () => {
    const pool = [{ id: 1 }, { id: 2 }]
    // 必须用 toContainEqual 而非 toContain：toContain 对对象是按**引用**比较的，
    // 而 pickEntry 刻意返回副本（见下一条测试），用 toContain 会与之直接矛盾。
    expect(pool).toContainEqual(pickEntry(pool, makeRng(1)))
  })

  it('空池返回 null', () => {
    expect(pickEntry([], makeRng(1))).toBeNull()
  })

  it('不返回原对象引用（防止下游修改污染配置）', () => {
    const pool = [{ id: 1, meta: { n: 1 } }]
    const got = pickEntry(pool, makeRng(1))
    expect(got).not.toBe(pool[0])
    expect(got).toEqual(pool[0])
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npx vitest run src/core/copyEngine.test.js
```

Expected: FAIL —— `Failed to resolve import "./copyEngine.js"`

- [ ] **Step 3: 实现 `copyEngine.js`**

```js
// src/core/copyEngine.js
import { pickMany, pickOne } from './rng.js'

const VAR_RE = /\{(\w+)\}/g

/**
 * 替换文案中的 {key} 占位。未知 key 原样保留，便于运营发现拼写错误。
 * 注意：本函数只做字符串替换，不解析 HTML。渲染层走 Vue 的 {{ }} 自动转义。
 */
export function interpolate(text, vars = {}) {
  if (!text) return ''
  return String(text).replace(VAR_RE, (match, key) =>
    Object.prototype.hasOwnProperty.call(vars, key) ? String(vars[key]) : match
  )
}

/** 从字符串池中不重复地取 n 条 */
export function pickText(pool, rng, n = 1) {
  if (!pool || pool.length === 0 || n <= 0) return []
  return pickMany(rng, pool, n)
}

/** 从对象池中取一条，返回浅拷贝，避免下游修改污染配置 */
export function pickEntry(pool, rng) {
  if (!pool || pool.length === 0) return null
  const picked = pickOne(rng, pool)
  return picked ? { ...picked } : null
}
```

`pickEntry` 返回拷贝而非引用 —— 配置对象是模块级单例，下游一旦就地修改会污染后续所有请求。

- [ ] **Step 4: 运行测试确认通过**

```bash
npx vitest run src/core/copyEngine.test.js
```

Expected: PASS

- [ ] **Step 5: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 12: 文案库内容

**Files:**
- Create: `src/config/copy/zodiac.json`, `tenure.json`, `department.json`, `identity.json`, `scores.json`, `fortunes.json`, `lucky.json`, `ui.json`, `src/config/lint-rules.json`
- Test: `src/core/copyLibrary.test.js`
- Modify: `src/core/config.js`（追加文案库一致性校验）

**Interfaces:**
- Consumes: 无
- Produces: 8 个 JSON 数据文件，被 `config.js` 装配进 `runtimeConfig.copy`

**风格硬约束（每个文件都适用）**：
- 单条 **≤ 60 字**（按 `[...str].length` 计数）
- 只自嘲、不嘲人；不出现具体同事名、领导名、真实项目名
- 巴纳姆效应优先（人人对得上才叫准）
- 禁用词见 Global Constraints；**`工位风水` 例外**

- [ ] **Step 1: 创建 `src/config/copy/zodiac.json`**

结构：12 个星座 key，每个含 `roasts` 数组，**每星座 3 条**。

```json
{
  "aries": {
    "roasts": [
      "你的行动力全公司第一，方向感全公司倒数第一。上午刚立完 flag，下午已经在改需求了。",
      "别人开会是在对齐，你开会是在点火。散会时大家都记住了你的态度，没人记住你的方案。",
      "你最擅长的职场技能是「先干起来再说」，最不擅长的是「干之前看一眼日历」。"
    ]
  },
  "taurus": {
    "roasts": [
      "你的工位是全公司最像家的地方——零食、靠垫、保温杯一应俱全，就是活儿推得有点慢。",
      "你对「稳定」的执念，让每次组织架构调整都像在动你的祖坟。",
      "别人追风口，你追性价比。这份定力放在投资上是优点，放在周报上是缺点。"
    ]
  },
  "gemini": {
    "roasts": [
      "你的大脑是公司的分布式服务器——上午写方案，下午写周报，晚上还能帮同事修电脑。建议把「多线程」写进绩效自评。",
      "你一个人能同时主持三个话题，但没有一个能撑到结论。",
      "你的兴趣周期是两周，正好等于一个需求迭代的周期。巧了，也正好是你放弃它的时间。"
    ]
  },
  "cancer": {
    "roasts": [
      "你把同事随口一句「这个再改改」记了整整一周，还顺手做了三个备选方案。",
      "你的共情能力极强，强到甲方一个「嗯」你就能脑补出一整套不满。",
      "你最会照顾所有人的情绪，除了你自己的。"
    ]
  },
  "leo": {
    "roasts": [
      "你的周报写得像获奖感言，可惜观众只有你自己和那个从不点开附件的领导。",
      "你在群里发消息自带追光灯，可惜今天群里只有三个人在线。",
      "你适合当主讲人，不适合当那个负责整理会议纪要的人。"
    ]
  },
  "virgo": {
    "roasts": [
      "你的文档排版精美、目录完整、版本号规范——然后没人看。",
      "你能在别人的 PPT 里一眼发现字号不一致，却看不见自己已经连着加班两周了。",
      "你改的不是 bug，是别人的人生。"
    ]
  },
  "libra": {
    "roasts": [
      "你的「都行」是真心的，但你的内心其实已经排好了优先级，只是不肯说。",
      "你在两个方案之间反复权衡的样子，像极了在工位前纠结午饭吃什么的自己。",
      "你最怕的不是做错决定，是有人问你「你想选哪个」。"
    ]
  },
  "scorpio": {
    "roasts": [
      "你表面上配合得天衣无缝，内心已经为这个方案准备了 108 个反驳角度。",
      "你记性好得可怕——三个月前谁在群里说过什么，你都还留着截图。",
      "你不轻易表态，一表态就是终局。"
    ]
  },
  "sagittarius": {
    "roasts": [
      "你的思路像脱缰的野马，可惜跑到一半发现没带缰绳的那个人是你自己。",
      "你立项的速度是别人的三倍，收尾的速度是别人的三分之一。",
      "你说「这个简单」的时候，全组人都默默打开了排期表。"
    ]
  },
  "capricorn": {
    "roasts": [
      "你把加班当成一种修行，把 KPI 当成一种信仰，把休假当成一种背叛。",
      "你是全组最靠谱的人，也是全组最不会说「我不行」的人。",
      "你的年度计划精确到周，唯一没算进去的变量是你自己会累。"
    ]
  },
  "aquarius": {
    "roasts": [
      "你的想法总是领先时代三年，可惜公司的排期只看到这个季度。",
      "你提出方案的方式很酷，酷到没人敢接话。",
      "你不喜欢被规则束缚，但你也确实需要有人提醒你交周报。"
    ]
  },
  "pisces": {
    "roasts": [
      "你的想象力能撑起一整个发布会，执行力只够撑起一页 PPT。",
      "别人在开会，你在神游；等你神游回来，方案已经定完了——而且比你想的那个差。",
      "你最容易被打动，也最容易被打断。"
    ]
  }
}
```

- [ ] **Step 2: 创建 `src/config/copy/tenure.json`**

```json
{
  "lt1": [
    "你还在用「我不太清楚，我去问一下」解决所有问题——这句话现在是护身符，明年就是减分项了。",
    "你交出去的每一份东西都带着一种小心翼翼的礼貌，像刚搬进宿舍的室友。"
  ],
  "1to3": [
    "你已经学会了在群里不说话，但还没学会在群里该说话的时候说话。",
    "你现在的能力足够发现问题，权力还不够解决问题，于是你练就了一项绝技叫「看着它发生」。"
  ],
  "3to5": [
    "已经过了「什么都要问」的阶段，却还没到「可以骂新人」的级别，这段时间的你，主打一个「沉默地懂」。",
    "你是组里那个「问他就对了」的人，也是组里那个「没人问他累不累」的人。"
  ],
  "gt5": [
    "你是公司的活化石，见证过三任领导、四套流程、五种 OKR 写法，并且对它们全部持保留意见。",
    "新人看你的眼神像看一本合上的书——知道里面有东西，但不知道从哪翻起。"
  ]
}
```

- [ ] **Step 3: 创建 `src/config/copy/department.json`**

key 必须是 6 个大类，**每类 3 条**。

```json
{
  "tech": [
    "你的工位风水：显示器朝北，bug 朝南，重启之后一切如愿。",
    "你和产品的关系，就像解释器和编译器——都能跑，但谁也不想迁就谁。",
    "你写的注释只有两种：一种是「这里为什么这么写」，另一种是「我也不知道为什么这么写」。"
  ],
  "product": [
    "你画的每一个流程图，最后都会在开发那里变成一句「这个做不了」。",
    "你的原型图改到第 8 版时，已经没人记得第 1 版长什么样了，包括你自己。",
    "你最常说的一句话是「就改一点点」，最怕听到的一句话也是「就改一点点」。"
  ],
  "market": [
    "你的 PPT 里每个数字都很漂亮，漂亮到财务想找你聊聊。",
    "你能把任何产品讲出一个动人的故事，唯独讲不清这个季度的完成率。",
    "你见客户时是春风，回工位时是台风。"
  ],
  "ops": [
    "你是全公司情绪最稳定的人，因为你的工作就是替别人的情绪兜底。",
    "用户的一句「在吗」，能让你的心率瞬间上到 120。",
    "你的日常是在两种状态之间切换：正在处理投诉，和即将处理投诉。"
  ],
  "admin": [
    "你是全公司唯一记得所有人的生日、入职日和工位号的人，也是唯一没被记进任何项目复盘的人。",
    "你经手过所有流程，也被所有流程绕过。",
    "别人问你「这个能不能报」，你回答「能」的时候比「不能」的时候还紧张。"
  ],
  "other": [
    "你的岗位描述在公司组织架构图上，属于那个叫做「等」的位置。",
    "你每次自我介绍都要花三十秒解释自己到底做什么，最后对方还是点了点头说「哦，行政是吧」。",
    "你在公司里的存在感，和你在群里的发言频率一样，稳定而低调。"
  ]
}
```

- [ ] **Step 4: 创建 `src/config/copy/identity.json`**

**通用标签 22 条（`dept: null`）+ 每部门 3 条 = 18 条，合计 40 条。**

```json
[
  { "text": "会议隐身术大师", "dept": null },
  { "text": "日报文学青年", "dept": null },
  { "text": "带薪哲学家", "dept": null },
  { "text": "下午三点文艺复兴者", "dept": null },
  { "text": "周报押韵选手", "dept": null },
  { "text": "工位风水研究员", "dept": null },
  { "text": "带薪思考型打工人", "dept": null },
  { "text": "群消息已读不回艺术家", "dept": null },
  { "text": "deadline 前爆发型选手", "dept": null },
  { "text": "会议室占座永久居民", "dept": null },
  { "text": "需求变更承受体", "dept": null },
  { "text": "精神离职预备役", "dept": null },
  { "text": "下午茶首席品鉴官", "dept": null },
  { "text": "摸鱼节奏掌控者", "dept": null },
  { "text": "OKR 措辞优化专家", "dept": null },
  { "text": "电梯社交尴尬症患者", "dept": null },
  { "text": "键盘声氛围组组长", "dept": null },
  { "text": "情绪稳定的接锅侠", "dept": null },
  { "text": "年假余额守财奴", "dept": null },
  { "text": "工位零食战略储备官", "dept": null },
  { "text": "对齐颗粒度翻译官", "dept": null },
  { "text": "周五下午效率之神", "dept": null },
  { "text": "需求翻译官", "dept": "tech" },
  { "text": "注释考古学家", "dept": "tech" },
  { "text": "线上事故背锅预备役", "dept": "tech" },
  { "text": "PRD 措辞艺术家", "dept": "product" },
  { "text": "原型图无限迭代者", "dept": "product" },
  { "text": "用户访谈职业听众", "dept": "product" },
  { "text": "PPT 炼金术士", "dept": "market" },
  { "text": "客户情绪收纳箱", "dept": "market" },
  { "text": "季度目标讲故事的人", "dept": "market" },
  { "text": "投诉消化系统", "dept": "ops" },
  { "text": "用户情绪稳定器", "dept": "ops" },
  { "text": "24 小时在线的那个头像", "dept": "ops" },
  { "text": "全公司生日提醒器", "dept": "admin" },
  { "text": "流程活字典", "dept": "admin" },
  { "text": "报销单鉴定专家", "dept": "admin" },
  { "text": "组织架构图上的省略号", "dept": "other" },
  { "text": "自我介绍超长待机选手", "dept": "other" },
  { "text": "安静而重要的那个人", "dept": "other" }
]
```

- [ ] **Step 5: 创建 `src/config/copy/scores.json`**

结构：`维度 → 等级带 → 文案数组`，**每格 2 条**（4 维 × 4 档 × 2 = 32 条）。

```json
{
  "moYu": {
    "wei": [
      "你的摸鱼指数低到同事怀疑你是不是新来的——放松一点，公司不会因为你喝口水就倒闭。",
      "你连摸鱼都要挑时间，这份谨慎用在项目上早升职了。"
    ],
    "ping": [
      "你摸鱼摸得很有分寸，属于「领导路过时手一定在键盘上」的水平。",
      "你的摸鱼是间歇性的，像信号不好的 WiFi，时有时无。"
    ],
    "ji": [
      "你已掌握「显示器亮度调最低但眼睛还睁着」的进阶技巧。",
      "你的摸鱼节奏稳定，属于可持续性的那种。"
    ],
    "daji": [
      "你的摸鱼已经进入化境——别人摸鱼被看见，你摸鱼被写进季度总结。",
      "你摸的不是鱼，是时间管理。建议开个分享会。"
    ]
  },
  "shengZhi": {
    "wei": [
      "今天的升职运像电梯停在负一层，门开了但没人进来。",
      "别急着表现，今天你说话的分量约等于群里那个「收到」。"
    ],
    "ping": [
      "升职运平稳，适合把手上的事做完，而不是新开一个坑。",
      "今天不宜主动请缨，宜把手头的事做得让人挑不出毛病。"
    ],
    "ji": [
      "今天适合在你擅长的事上多露一手，会有人看见。",
      "你的名字今天有机会被某个重要的人提起，记得保持在线。"
    ],
    "daji": [
      "今天的你自带主角光环，走哪都有人问「这个你熟吗」。",
      "升职运满格，今天你说的每一句「我觉得」都值得被记下来。"
    ]
  },
  "renMai": {
    "wei": [
      "今天的人脉运提示你：少说话，多点头，别在群里发表情包。",
      "今天不宜主动社交，宜安静地把午饭吃完。"
    ],
    "ping": [
      "人脉运平稳，适合维护老关系，不适合认识新朋友。",
      "今天遇到的人都是点头之交，但那声「早」值得说。"
    ],
    "ji": [
      "今天适合跟久没联系的人打个招呼，会有意外的回音。",
      "人脉运不错，茶水间偶遇的几率显著上升。"
    ],
    "daji": [
      "今天的贵人可能就藏在你刚回复的那条消息里。",
      "人脉运爆棚，今天认识的人明年可能会成为你的资源。"
    ]
  },
  "caiYun": {
    "wei": [
      "今天的财运适合守，不适合创——比如今天就别点那杯加料的奶茶了。",
      "今天不宜冲动消费，宜看看余额。"
    ],
    "ping": [
      "财运平平，宜记账，不宜跟风。",
      "今天不会有额外开销，也不会有意外之喜，属于安稳的一天。"
    ],
    "ji": [
      "财运不错，今天适合把该报销的报销了，该要的钱要了。",
      "今天有小财入账的可能，来源大概率是一笔你忘了的钱。"
    ],
    "daji": [
      "财运亨通，今天的你适合谈钱——无论是薪资还是预算。",
      "今天的财运让你在付款时格外从容。建议低调。"
    ]
  }
}
```

- [ ] **Step 6: 创建 `src/config/copy/fortunes.json`**

结构：数组，每支含 `level` / `verse` / `yi` / `ji` / `explain`。**共 24 支**，档位分布为 上上签 2 / 大吉 3 / 中吉 6 / 小吉 6 / 平 4 / 小凶 3。

```json
[
  {
    "level": "上上签",
    "verse": "风来满座皆春色，一举成名天下知。",
    "yi": "主动请缨",
    "ji": "过度谦虚",
    "explain": "今天你开口的每一句，都比你以为的更有分量。"
  },
  {
    "level": "上上签",
    "verse": "久旱逢得及时雨，枯木枝头又著花。",
    "yi": "推进卡住的事",
    "ji": "继续等",
    "explain": "那件拖了很久的事，今天推一把就会动。"
  },
  {
    "level": "大吉",
    "verse": "拨云见日天光好，前路分明不用猜。",
    "yi": "把想法说出来",
    "ji": "藏着掖着",
    "explain": "你纠结的那个问题，答案今天会自己浮出来。"
  },
  {
    "level": "大吉",
    "verse": "积土成山风雨兴，功夫到处自然成。",
    "yi": "收尾旧事",
    "ji": "另起炉灶",
    "explain": "你之前白做的那部分，今天会派上用场。"
  },
  {
    "level": "大吉",
    "verse": "灯前落笔皆如意，案上文书件件成。",
    "yi": "写周报",
    "ji": "口头汇报",
    "explain": "今天写下来的东西，比说出来的更有说服力。"
  },
  {
    "level": "中吉",
    "verse": "茶凉之前事已成，键盘声里见分明。",
    "yi": "主动同步进度",
    "ji": "默默改需求",
    "explain": "今日贵人就在你刚 @ 的那个群里。"
  },
  {
    "level": "中吉",
    "verse": "缓步慢行终到岸，不争先后自安然。",
    "yi": "按自己的节奏来",
    "ji": "跟人比进度",
    "explain": "今天慢一点反而比较快。"
  },
  {
    "level": "中吉",
    "verse": "旧事重提须细看，翻开尚有未了缘。",
    "yi": "回看聊天记录",
    "ji": "想当然",
    "explain": "你以为定好的事，其实还有一处没对齐。"
  },
  {
    "level": "中吉",
    "verse": "半盏清茶消长日，一封短讯解千愁。",
    "yi": "把话问清楚",
    "ji": "自己猜",
    "explain": "一句「我确认下」今天能省你两小时。"
  },
  {
    "level": "中吉",
    "verse": "莫道今朝无所得，回头已是万重山。",
    "yi": "复盘",
    "ji": "急着开新坑",
    "explain": "你今天会突然想通一件上个月的事。"
  },
  {
    "level": "中吉",
    "verse": "座上新朋携旧意，言谈之间见真心。",
    "yi": "多听少说",
    "ji": "抢话",
    "explain": "今天有人会给你一个没明说的提示。"
  },
  {
    "level": "小吉",
    "verse": "轻舟已过小重山，前路平平未有澜。",
    "yi": "把手头的事做完",
    "ji": "临时加戏",
    "explain": "今天没有惊喜，但也没有意外，挺好。"
  },
  {
    "level": "小吉",
    "verse": "纸上得来终觉浅，躬行方知此事难。",
    "yi": "先动手试",
    "ji": "继续调研",
    "explain": "今天写十行代码比看十页文档有用。"
  },
  {
    "level": "小吉",
    "verse": "闲来翻检旧文档，字里行间有意藏。",
    "yi": "翻旧资料",
    "ji": "重新造轮子",
    "explain": "你要找的答案，同事三个月前就写过了。"
  },
  {
    "level": "小吉",
    "verse": "午后斜阳照案头，一刻清闲抵万金。",
    "yi": "给自己留点空",
    "ji": "把日程排满",
    "explain": "今天最好的产出，来自你发呆的那十分钟。"
  },
  {
    "level": "小吉",
    "verse": "同行三五皆良伴，何必孤身挑重担。",
    "yi": "开口求助",
    "ji": "硬扛",
    "explain": "今天你张一次口，能省半天的力气。"
  },
  {
    "level": "小吉",
    "verse": "灯下细看无差误，一纸文书抵万言。",
    "yi": "检查一遍再交",
    "ji": "手快",
    "explain": "你差点发出去的那份东西，再看一眼会发现问题。"
  },
  {
    "level": "平",
    "verse": "守得云开见月明，不急不躁自安宁。",
    "yi": "稳住",
    "ji": "轻举妄动",
    "explain": "今天适合守成，不适合开疆。"
  },
  {
    "level": "平",
    "verse": "风平浪静舟自稳，何必扬帆赶潮流。",
    "yi": "按部就班",
    "ji": "跟风改方案",
    "explain": "别人改方向是别人的事，你今天的路线是对的。"
  },
  {
    "level": "平",
    "verse": "静坐常思己之事，闲谈莫论他人非。",
    "yi": "专注自己的活",
    "ji": "参与群聊讨论",
    "explain": "今天群里的热闹，跟你没什么关系。"
  },
  {
    "level": "平",
    "verse": "蓄力待时非无为，厚积薄发在明朝。",
    "yi": "整理与归档",
    "ji": "急于证明自己",
    "explain": "今天做的整理工作，下周会救你一次。"
  },
  {
    "level": "小凶",
    "verse": "群中消息且休看，明朝再议未为迟。",
    "yi": "把手机扣过来",
    "ji": "秒回",
    "explain": "那条让你血压升高的消息，明天看会顺眼很多。"
  },
  {
    "level": "小凶",
    "verse": "莫在此时论长短，且待明朝再说理。",
    "yi": "晚点再回复",
    "ji": "当场表态",
    "explain": "今天你脱口而出的那句话，明天需要你再解释一遍。"
  },
  {
    "level": "小凶",
    "verse": "手快常有三分错，缓一步便少一桩。",
    "yi": "发出前再看一眼",
    "ji": "手速",
    "explain": "今天你的手速会给你带来一点小麻烦，不大，但够烦。"
  }
]
```

- [ ] **Step 7: 创建 `src/config/copy/lucky.json`**

```json
{
  "colors": [
    "雾霾蓝", "焦糖拿铁色", "旧键盘灰", "会议室白板绿", "荧光便签黄",
    "快递纸箱棕", "显示器熄屏黑", "加班夜灯橙", "工牌挂绳红", "茶水间杯子青",
    "地铁扶手银", "奶茶三分糖色"
  ],
  "numbers": [1, 2, 3, 5, 6, 7, 8, 9, 12],
  "directions": [
    "茶水间", "窗边第二个工位", "打印机旁", "北向会议室", "工位左手边第三个抽屉",
    "电梯口", "楼下便利店", "你自己的座位"
  ],
  "zodiacs": [
    "白羊座", "金牛座", "双子座", "巨蟹座", "狮子座", "处女座",
    "天秤座", "天蝎座", "射手座", "摩羯座", "水瓶座", "双鱼座"
  ]
}
```

- [ ] **Step 8: 创建 `src/config/copy/ui.json`**

```json
{
  "intro": {
    "tagline": "打工人能量图鉴",
    "desc1": "填 4 个空，摇一支签",
    "desc2": "看看今天的你，是哪种职场生物"
  },
  "divining": {
    "title": "正在为你摇签",
    "steps": ["静心", "摇签", "落定"],
    "loading": "正在为您绘制天命海报…"
  },
  "result": {
    "identityLabel": "你的职场身份是",
    "scoresLabel": "四维能量",
    "roastLabel": "扎心时刻",
    "deptLabel": "你的部门玄学",
    "fortuneLabel": "你抽到的是",
    "luckyLabel": "今日彩蛋",
    "yiLabel": "宜",
    "jiLabel": "忌",
    "luckyColor": "幸运色",
    "luckyNumber": "幸运数字",
    "luckyDirection": "贵人方位",
    "luckyZodiac": "贵人星座",
    "again": "再算一次"
  },
  "lock": {
    "gateTitle": "完整解读已生成",
    "gateHint": "输入今日口令解锁",
    "gatePlaceholder": "今日口令",
    "gateButton": "解 锁",
    "gateError": "口令不对哦，今日口令请向摊主获取",
    "gateRetry": "再看看口令，就在摊位易拉宝上",
    "lockedHint": "解锁后可见"
  }
}
```

`lock` 段本计划不使用，计划二的口令解锁会用到。现在就放进去，避免运营两次改同一个文件。

- [ ] **Step 9: 创建合规规则单一来源 `src/config/lint-rules.json`**

禁用词表与长度上限在「单元测试」和「构建前置 linter」两处都要用。放成一个 JSON，两处 import 它，避免逐字重复。

```json
{
  "maxLength": 60,
  "banned": [
    "算命", "占卜", "改运", "改命", "命理", "前世", "因果", "化解", "生辰八字",
    "破财", "破产", "失业", "被裁", "裁员", "生病", "疾病", "官司",
    "口舌", "车祸", "死亡", "血光", "离婚", "分手"
  ],
  "placeholderMark": "【待填写"
}
```

> **为何没有 `allowedPhrases`（早期版本曾有一个 `["工位风水"]` 例外）**：
> 实测证明它**没有任何作用，且唯一可达的效果是有害的** ——
> ① 23 个禁用词中没有任何一个与「工位风水」存在字符重叠，故掩码从不改变任何结果
> （对全部 370 条文案做过对照，判定差异为 0）；
> ② 掩码是**先删除再匹配**，它只可能**跨越切口凭空造出命中**：`破工位风水财`
> 原本不含连续子串「破财」，掩码后反而变成「破财」而误报。
> 即该机制**不能抑制真命中，只能制造假阳性** —— 在一个阻断式构建闸门里，
> 这是纯负价值。已整体移除，连带移除两条无法失败的「例外」断言。
>
> 注意：PRD 6.2 从未把「风水」列入禁用词，PRD 附录 A 自身就以「工位风水研究员」
> 为正面示例。所以这里**本就没有需要豁免的东西**。若将来真把「风水」加进禁用词表，
> 届时应连同一条**能失败**的例外测试一起加回，而不是恢复一个惰性机制。

- [ ] **Step 10: 写文案库校验测试**

```js
// src/core/copyLibrary.test.js
import { describe, it, expect } from 'vitest'
import zodiac from '../config/copy/zodiac.json'
import tenure from '../config/copy/tenure.json'
import department from '../config/copy/department.json'
import identity from '../config/copy/identity.json'
import scores from '../config/copy/scores.json'
import fortunes from '../config/copy/fortunes.json'
import lucky from '../config/copy/lucky.json'
import ui from '../config/copy/ui.json'
import rules from '../config/lint-rules.json'
import { ZODIACS, TENURES } from './config.js'

const DEPT_KEYS = ['tech', 'product', 'market', 'ops', 'admin', 'other']
const DIMS = ['moYu', 'shengZhi', 'renMai', 'caiYun']
const BAND_KEYS = ['wei', 'ping', 'ji', 'daji']
const LEVELS = ['上上签', '大吉', '中吉', '小吉', '平', '小凶']

const len = (s) => [...s].length

/** 递归收集 JSON 中所有字符串 */
function collectStrings(node, out = []) {
  if (typeof node === 'string') out.push(node)
  else if (Array.isArray(node)) node.forEach((n) => collectStrings(n, out))
  else if (node && typeof node === 'object') Object.values(node).forEach((n) => collectStrings(n, out))
  return out
}

const ALL_COPY = [zodiac, tenure, department, identity, scores, fortunes, lucky, ui]

describe('通用约束', () => {
  it(`单条文案不超过 ${rules.maxLength} 字`, () => {
    const tooLong = []
    for (const file of ALL_COPY) {
      for (const s of collectStrings(file)) {
        if (len(s) > rules.maxLength) tooLong.push(s)
      }
    }
    expect(tooLong).toEqual([])
  })

  it('不含禁用词（去空白后匹配，与 linter 同一套规则）', () => {
    // 与 scripts/lint-copy.js 的 SQUEEZE 必须一致 —— 两边算法漂移
    // 就等于存在两套标准，而「单一来源」正是 lint-rules.json 存在的理由。
    const hits = []
    for (const file of ALL_COPY) {
      for (const s of collectStrings(file)) {
        const squeezed = s.replace(/\s+/g, '')
        for (const w of rules.banned) if (squeezed.includes(w)) hits.push(`"${w}" in "${s}"`)
      }
    }
    expect(hits).toEqual([])
  })
})

describe('zodiac.json', () => {
  it('12 个星座齐备', () => {
    expect(Object.keys(zodiac).sort()).toEqual([...ZODIACS].sort())
  })

  it('每个星座 3 条吐槽', () => {
    for (const z of ZODIACS) expect(zodiac[z].roasts).toHaveLength(3)
  })

  it('同一星座内 3 条互不相同', () => {
    for (const z of ZODIACS) expect(new Set(zodiac[z].roasts).size).toBe(3)
  })
})

describe('tenure.json', () => {
  it('4 段年限齐备且各有文案', () => {
    expect(Object.keys(tenure).sort()).toEqual([...TENURES].sort())
    for (const t of TENURES) expect(tenure[t].length).toBeGreaterThanOrEqual(1)
  })
})

describe('department.json', () => {
  it('6 个大类齐备，每类至少 3 条', () => {
    expect(Object.keys(department).sort()).toEqual([...DEPT_KEYS].sort())
    for (const k of DEPT_KEYS) expect(department[k].length).toBeGreaterThanOrEqual(3)
  })
})

describe('identity.json', () => {
  it('共 40 条，每部门恰好 3 条专属', () => {
    expect(identity).toHaveLength(40)
    for (const k of DEPT_KEYS) {
      expect(identity.filter((i) => i.dept === k)).toHaveLength(3)
    }
  })

  it('通用标签不少于 20 条', () => {
    expect(identity.filter((i) => i.dept === null).length).toBeGreaterThanOrEqual(20)
  })

  it('每个部门都能凑出至少 20 个候选（专属 + 通用）', () => {
    const generic = identity.filter((i) => i.dept === null).length
    for (const k of DEPT_KEYS) {
      const own = identity.filter((i) => i.dept === k).length
      expect(own + generic).toBeGreaterThanOrEqual(20)
    }
  })

  it('dept 字段只能是 null 或 6 大类之一', () => {
    for (const i of identity) {
      expect(i.dept === null || DEPT_KEYS.includes(i.dept)).toBe(true)
    }
  })

  it('标签文字不重复', () => {
    const texts = identity.map((i) => i.text)
    expect(new Set(texts).size).toBe(texts.length)
  })
})

describe('scores.json', () => {
  it('4 维 × 4 档齐备，每格至少 2 条', () => {
    expect(Object.keys(scores).sort()).toEqual([...DIMS].sort())
    for (const dim of DIMS) {
      expect(Object.keys(scores[dim]).sort()).toEqual([...BAND_KEYS].sort())
      for (const b of BAND_KEYS) expect(scores[dim][b].length).toBeGreaterThanOrEqual(2)
    }
  })
})

describe('fortunes.json', () => {
  it('24 支签', () => {
    expect(fortunes).toHaveLength(24)
  })

  it('level 只能是 6 个合法标签，绝不出现大凶', () => {
    for (const f of fortunes) {
      expect(LEVELS).toContain(f.level)
      expect(f.level).not.toBe('大凶')
    }
  })

  it('六档全部有签可抽', () => {
    for (const lv of LEVELS) {
      expect(fortunes.filter((f) => f.level === lv).length).toBeGreaterThanOrEqual(2)
    }
  })

  it('每支签四个字段齐备且非空', () => {
    for (const f of fortunes) {
      for (const k of ['verse', 'yi', 'ji', 'explain']) {
        expect(typeof f[k]).toBe('string')
        expect(f[k].length).toBeGreaterThan(0)
      }
    }
  })

  it('签辞不重复', () => {
    const verses = fortunes.map((f) => f.verse)
    expect(new Set(verses).size).toBe(verses.length)
  })
})

describe('lucky.json', () => {
  it('彩蛋池数量符合预期', () => {
    expect(lucky.colors).toHaveLength(12)
    expect(lucky.numbers).toHaveLength(9)
    expect(lucky.directions).toHaveLength(8)
    expect(lucky.zodiacs).toHaveLength(12)
  })

  it('数字全为 1~99 的整数', () => {
    for (const n of lucky.numbers) {
      expect(Number.isInteger(n)).toBe(true)
      expect(n).toBeGreaterThan(0)
      expect(n).toBeLessThan(100)
    }
  })
})

describe('ui.json', () => {
  it('四个区块齐备', () => {
    for (const k of ['intro', 'divining', 'result', 'lock']) {
      expect(ui[k]).toBeTruthy()
    }
  })

  it('动画文案为 3 步', () => {
    expect(ui.divining.steps).toHaveLength(3)
  })
})
```

- [ ] **Step 11: 运行测试确认通过**

```bash
npx vitest run src/core/copyLibrary.test.js
```

Expected: PASS。若有失败，按错误信息逐条修正对应 JSON（通常是某条文案超了 60 字或命中了禁用词）。

- [ ] **Step 12: 把文案库接入 `config.js` 并追加一致性校验**

在 `src/core/config.js` 顶部追加导入：

```js
import copyZodiac from '../config/copy/zodiac.json'
import copyTenure from '../config/copy/tenure.json'
import copyDepartment from '../config/copy/department.json'
import copyIdentity from '../config/copy/identity.json'
import copyScores from '../config/copy/scores.json'
import copyFortunes from '../config/copy/fortunes.json'
import copyLucky from '../config/copy/lucky.json'
import copyUi from '../config/copy/ui.json'

const copy = {
  zodiac: copyZodiac,
  tenure: copyTenure,
  department: copyDepartment,
  identity: copyIdentity,
  scores: copyScores,
  fortunes: copyFortunes,
  lucky: copyLucky,
  ui: copyUi
}
```

在 `runtimeConfig` 对象中追加 `copy` 字段（即成为 `copy` 属性）。

把 `checkConfigConsistency` 替换为：

```js
export function checkConfigConsistency() {
  const problems = []
  const deptKeys = Object.keys(departments)
  const bonusKeys = Object.keys(scoring.deptBonus || {})

  for (const k of deptKeys) {
    if (!bonusKeys.includes(k)) problems.push(`scoring.deptBonus 缺少部门 "${k}"`)
    if (!copy.department[k]) problems.push(`copy/department.json 缺少部门 "${k}"`)
  }
  for (const k of bonusKeys) {
    if (!deptKeys.includes(k)) problems.push(`departments.json 缺少部门 "${k}"`)
  }
  for (const z of ZODIACS) {
    if (!copy.zodiac[z]) problems.push(`copy/zodiac.json 缺少星座 "${z}"`)
  }
  for (const t of TENURES) {
    if (!copy.tenure[t]) problems.push(`copy/tenure.json 缺少年限段 "${t}"`)
  }
  for (const dim of scoring.dimensions) {
    if (!copy.scores[dim]) problems.push(`copy/scores.json 缺少维度 "${dim}"`)
  }
  return problems
}
```

- [ ] **Step 13: 运行全部测试**

```bash
npx vitest run
```

Expected: 全部 PASS，包括 `config.test.js` 的 `checkConfigConsistency()` 返回空数组。

- [ ] **Step 14: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 13: 签文与结果组装 `fortune.js`

**Files:**
- Create: `src/core/fortune.js`
- Test: `src/core/fortune.test.js`

**Interfaces:**
- Consumes: `scoring.js` 的 `computeScores` / `averageScore`、`copyEngine.js` 的 `pickText` / `pickEntry` / `interpolate`、`rng.js`
- Produces:
  - `BASE_WEIGHT`、`BIAS` 常量
  - `biasFor(avg): Record<level, number>`
  - `pickLevel(avg, rng): string`
  - `drawFortune(avg, pool, rng): { level, verse, yi, ji, explain }`
  - `drawIdentity(profile, identityPool, rng): { text, dept }`
  - `buildReading(profile, cfg, rng): reading`
  - `buildReading` 返回：`scores`、`bands`、`avg`、`identity`、`roastTexts`、`deptJoke`、`fortune`、`lucky`、`vars`
    - **字段名是 `roastTexts`，不是 `roasts`**（Task 17 读的正是 `r.roastTexts`）。
    - **不含 `seed`**：随机种子由 Task 15 自行计算并通过 `setReading({ ...reading, seed })` 附加到 store 上，
      不由 `buildReading` 产出。这一区分是有意的 —— `buildReading` 只负责内容，种子的生命周期由调用方管理。

- [ ] **Step 1: 写失败测试**

```js
// src/core/fortune.test.js
import { describe, it, expect } from 'vitest'
import {
  BASE_WEIGHT, biasFor, pickLevel, drawFortune,
  drawIdentity, buildReading
} from './fortune.js'
import { makeRng } from './rng.js'
import { runtimeConfig } from './config.js'

const cfg = runtimeConfig
const DIMS = cfg.scoring.dimensions
const profile = { name: '张三', dept: 'tech', tenure: '1to3', zodiac: 'gemini' }

describe('BASE_WEIGHT', () => {
  it('六档齐备且不含大凶', () => {
    expect(Object.keys(BASE_WEIGHT).sort()).toEqual(
      ['上上签', '大吉', '中吉', '小吉', '平', '小凶'].sort()
    )
    expect(BASE_WEIGHT['大凶']).toBeUndefined()
  })

  it('好签档合计 20%、中吉小吉 45%、平 25%、小凶 10%', () => {
    const total = Object.values(BASE_WEIGHT).reduce((a, b) => a + b, 0)
    expect(total).toBe(100)
    expect((BASE_WEIGHT['上上签'] + BASE_WEIGHT['大吉']) / total).toBeCloseTo(0.2, 5)
    expect((BASE_WEIGHT['中吉'] + BASE_WEIGHT['小吉']) / total).toBeCloseTo(0.45, 5)
    expect(BASE_WEIGHT['平'] / total).toBeCloseTo(0.25, 5)
    expect(BASE_WEIGHT['小凶'] / total).toBeCloseTo(0.1, 5)
  })
})

describe('biasFor', () => {
  it('均分 ≥ 80 时好签系数大于 1、小凶小于 1', () => {
    const b = biasFor(85)
    expect(b['上上签']).toBeGreaterThan(1)
    expect(b['小凶']).toBeLessThan(1)
  })

  it('均分 70~76 时全部为 1（PRD 基准档）', () => {
    const b = biasFor(73)
    Object.values(b).forEach((v) => expect(v).toBe(1))
  })

  it('均分 < 70 时小凶系数大于 1、好签小于 1', () => {
    const b = biasFor(65)
    expect(b['小凶']).toBeGreaterThan(1)
    expect(b['上上签']).toBeLessThan(1)
  })

  it('三档边界按实测分布归位：77 与 70', () => {
    // 阈值来自对实际可达均分区间的穷尽测量（64~83），不是拍脑袋定的。
    expect(biasFor(77)['上上签']).toBeGreaterThan(1) // 上界含 77
    expect(biasFor(76.9)['上上签']).toBe(1) // 77 以下为基准档
    expect(biasFor(70)['上上签']).toBe(1) // 下界含 70
    expect(biasFor(69.9)['上上签']).toBeLessThan(1) // 70 以下为低档
  })
})

describe('pickLevel', () => {
  it('返回值恒为合法标签', () => {
    const rng = makeRng(1)
    for (let i = 0; i < 500; i++) {
      expect(Object.keys(BASE_WEIGHT)).toContain(pickLevel(70, rng))
    }
  })

  it('偏置生效：均分 90 的样本里好签比例高于均分 40', () => {
    const good = (avg) => {
      const rng = makeRng(7)
      let n = 0
      const N = 4000
      for (let i = 0; i < N; i++) {
        const lv = pickLevel(avg, rng)
        if (lv === '上上签' || lv === '大吉') n++
      }
      return n / N
    }
    expect(good(90)).toBeGreaterThan(good(40))
  })

  it('均分 85 时小凶概率约 2.7%（反差保留但稀有）', () => {
    const rng = makeRng(99)
    let n = 0
    const N = 20000
    for (let i = 0; i < N; i++) if (pickLevel(85, rng) === '小凶') n++
    const p = n / N
    expect(p).toBeGreaterThan(0.01)
    expect(p).toBeLessThan(0.05)
  })
})

describe('drawFortune', () => {
  it('抽出的签必须属于 pickLevel 选中的档位', () => {
    // 这条断言必须能捕获「档位滤波器被删掉」。
    // 做法：用同一个种子分别跑 pickLevel 与 drawFortune —— 后者内部第一步就是
    // pickLevel，故两者选中的档位必然相同；随后 drawFortune 从**该档的候选池**抽取。
    // 若滤波器失效（source 退化为全池），抽到的签档位就会与 expectedLevel 不符，
    // 六分之五的概率直接失败。（早先那版只断言「在池中存在」+「不是大凶」，
    // 两条都恒真，删掉滤波器也不会红 —— 属于假验证。）
    for (let i = 0; i < 200; i++) {
      const seed = 1000 + i
      const expectedLevel = pickLevel(73, makeRng(seed))
      const f = drawFortune(73, cfg.copy.fortunes, makeRng(seed))
      expect(f.level).toBe(expectedLevel)
    }
  })

  it('返回的签在池中存在且绝不为大凶', () => {
    const rng = makeRng(5)
    for (let i = 0; i < 100; i++) {
      const f = drawFortune(73, cfg.copy.fortunes, rng)
      expect(cfg.copy.fortunes).toContainEqual(f)
      expect(f.level).not.toBe('大凶')
    }
  })

  it('四个字段齐备', () => {
    const f = drawFortune(70, cfg.copy.fortunes, makeRng(1))
    for (const k of ['level', 'verse', 'yi', 'ji', 'explain']) {
      expect(f[k]).toBeTruthy()
    }
  })

  it('返回的是拷贝，修改它不会污染配置', () => {
    const f = drawFortune(70, cfg.copy.fortunes, makeRng(1))
    f.verse = '被改过了'
    expect(cfg.copy.fortunes.some((x) => x.verse === '被改过了')).toBe(false)
  })
})

describe('drawIdentity', () => {
  it('只从本部门专属与通用标签中抽取', () => {
    const rng = makeRng(3)
    for (let i = 0; i < 300; i++) {
      const id = drawIdentity(profile, cfg.copy.identity, rng)
      expect(id.dept === null || id.dept === profile.dept).toBe(true)
    }
  })

  it('长期抽样中专属标签会出现（部门代入感生效）', () => {
    const rng = makeRng(3)
    let own = 0
    for (let i = 0; i < 500; i++) {
      if (drawIdentity(profile, cfg.copy.identity, rng).dept === profile.dept) own++
    }
    expect(own).toBeGreaterThan(0)
  })
})

describe('buildReading', () => {
  it('产出完整 reading 结构', () => {
    const r = buildReading(profile, cfg, makeRng(20260913))
    expect(Object.keys(r.scores).sort()).toEqual([...DIMS].sort())
    expect(Object.keys(r.bands).sort()).toEqual([...DIMS].sort())
    expect(r.identity.text).toBeTruthy()
    expect(r.fortune.level).toBeTruthy()
    expect(r.lucky.color).toBeTruthy()
    expect(Number.isInteger(r.lucky.number)).toBe(true)
    expect(r.lucky.direction).toBeTruthy()
    expect(r.lucky.zodiac).toBeTruthy()
  })

  it('吐槽为 2~3 句', () => {
    const r = buildReading(profile, cfg, makeRng(1))
    expect(r.roastTexts.length).toBeGreaterThanOrEqual(2)
    expect(r.roastTexts.length).toBeLessThanOrEqual(3)
  })

  it('吐槽内容互不重复', () => {
    const r = buildReading(profile, cfg, makeRng(1))
    expect(new Set(r.roastTexts).size).toBe(r.roastTexts.length)
  })

  it('部门梗来自本部门大类', () => {
    const r = buildReading(profile, cfg, makeRng(1))
    expect(cfg.copy.department[profile.dept]).toContain(r.deptJoke)
  })

  it('插值变量齐备', () => {
    const r = buildReading(profile, cfg, makeRng(1))
    expect(r.vars.name).toBe('张三')
    expect(r.vars.dept).toBe('技术 / 研发')
    expect(r.vars.zodiac).toBe('双子座')
    expect(r.vars.tenure).toBeTruthy()
  })

  it('相同种子产出完全一致', () => {
    const a = buildReading(profile, cfg, makeRng(555))
    const b = buildReading(profile, cfg, makeRng(555))
    expect(a).toEqual(b)
  })

  it('不同种子产出不同', () => {
    const a = buildReading(profile, cfg, makeRng(1))
    const b = buildReading(profile, cfg, makeRng(2))
    expect(a).not.toEqual(b)
  })

  it('抽到的签绝不等于大凶', () => {
    const rng = makeRng(2026)
    for (let i = 0; i < 300; i++) {
      expect(buildReading(profile, cfg, rng).fortune.level).not.toBe('大凶')
    }
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npx vitest run src/core/fortune.test.js
```

Expected: FAIL —— `Failed to resolve import "./fortune.js"`

- [ ] **Step 3: 实现 `fortune.js`**

```js
// src/core/fortune.js
import { computeScores, averageScore } from './scoring.js'
import { pickText } from './copyEngine.js'
import { pickOne } from './rng.js'

/** 基准权重，对应 PRD 5.2 的四档占比 20% / 45% / 25% / 10%，合计 100 */
export const BASE_WEIGHT = {
  上上签: 8,
  大吉: 12,
  中吉: 22,
  小吉: 23,
  平: 25,
  小凶: 10
}

export const FORTUNE_LEVELS = Object.keys(BASE_WEIGHT)

// ⚠️ 阈值必须落在**实际可达**的均分区间内 —— 这一点曾出错，务必读懂再改。
//
// 穷尽枚举实测（288 画像组合 × 13⁴ 抖动元组 = 8,225,568 次抽取，
// **逐维 clamp(Math.round(…), 0, 100) 后再取四维平均**，即精确复刻 computeScores）：
//   均分范围 64.00 ~ 83.00，均值 73.21，中位数 73.25。
//   （范围之所以这么窄，是因为 spec §6.3 的公平性要求把每个星座的四维和
//     归一到恰好 280（均分 70），再叠加幅度有限的年限/部门加成与 ±6 抖动。）
//
// 早先照 spec §7.1 写的 80 / 60 是**错的**：60 低于可达最小值，那一档永不触发（死代码）；
// 80 只在 **0.32%** 的抽取中触发 —— 等于整个偏置机制对 **99.68%** 的用户是惰性的。
// 更根本的是，该机制的设计目的（防止「四维全大吉 + 小凶签」自相矛盾）在此配置下
// **结构上不可能发生**：因公平性归一，任何星座都不可能有四维同时 ≥80
// （实测 8,225,568 次抽取中发生 0 次；可达最大均分 83.00，出现于 market/gt5/aries）。
//
// 现按实测分布重标定，三档占比 **7.41% / 83.13% / 9.46%**（精确值，未取整分类）。
// 低档比高档略多，这是结构性的：加成和接近 0 的画像（lt1+other）中心在 70、
// 最低可到 64 却几乎够不到 77，而成家和大的画像（gt5+market = +28）中心就在 77。
//
// ⚠️ 统计占比时必须用**未取整**的均分：pickLevel 收到的是原始浮点值，
//    avg 76.9 属基准档、77.0 才进高档。先 Math.round 再分桶会得出
//    10.7% / 82.9% / 6.4% 的偏差数字（低档被显著低估）。
//
// **若将来调整 scoring.json 的 jitter 或加成幅度，必须重跑测量并重标定本阈值** ——
// 否则又会退化成「某一档永不触发」或「某档过窄」。
const BIAS_TIERS = [
  {
    min: 77,
    w: { 上上签: 1.6, 大吉: 1.6, 中吉: 1.3, 小吉: 1.3, 平: 0.7, 小凶: 0.3 }
  },
  {
    min: 70,
    w: { 上上签: 1, 大吉: 1, 中吉: 1, 小吉: 1, 平: 1, 小凶: 1 }
  },
  {
    min: 0,
    w: { 上上签: 0.6, 大吉: 0.6, 中吉: 0.8, 小吉: 0.8, 平: 1.5, 小凶: 1.5 }
  }
]

/** 四维均分对应的档位偏置系数 */
export function biasFor(avg) {
  const tier = BIAS_TIERS.find((t) => avg >= t.min)
  return tier ? tier.w : BIAS_TIERS[BIAS_TIERS.length - 1].w
}

/** 加权抽档位：权重 = 基准权重 × 偏置系数 */
export function pickLevel(avg, rng) {
  const bias = biasFor(avg)
  const levels = FORTUNE_LEVELS
  const weights = levels.map((lv) => BASE_WEIGHT[lv] * bias[lv])

  let total = 0
  for (const w of weights) total += w
  if (total <= 0) return '平'

  let r = rng() * total
  for (let i = 0; i < levels.length; i++) {
    r -= weights[i]
    if (r < 0) return levels[i]
  }
  return levels[levels.length - 1]
}

/** 按均分偏置抽一支签，返回拷贝 */
export function drawFortune(avg, pool, rng) {
  const level = pickLevel(avg, rng)
  const candidates = pool.filter((f) => f.level === level)
  const source = candidates.length ? candidates : pool
  const picked = pickOne(rng, source)
  return picked ? { ...picked } : null
}

/** 从「本部门专属 + 通用」池中抽一个身份标签 */
export function drawIdentity(profile, identityPool, rng) {
  const pool = identityPool.filter((i) => i.dept === null || i.dept === profile.dept)
  const source = pool.length ? pool : identityPool
  const picked = pickOne(rng, source)
  return picked ? { ...picked } : { text: '安静的职场观察者', dept: null }
}

/**
 * 组装完整结果并冻结。
 * 调用一次即产出全部内容，之后只读 —— 保证页面内分数与签文绝不抖动。
 */
export function buildReading(profile, cfg, rng) {
  const { scores, bands } = computeScores(profile, cfg.scoring, rng)
  const avg = averageScore(scores, cfg.scoring.dimensions)

  const vars = {
    // name 也必须兜底。copyEngine.interpolate 内部用 String(vars[key])，
    // 值为 undefined 时会**原样渲染出字面量 "undefined"**（其余三个变量本就有 ?? ''，
    // 唯独 name 没有）。未走校验的调用路径会打印出「undefined 今天宜早退」。
    name: profile.name ?? '',
    dept: cfg.departments[profile.dept]?.label ?? '',
    zodiac: cfg.zodiacLabels[profile.zodiac] ?? '',
    tenure: cfg.tenureLabels[profile.tenure] ?? ''
  }

  const zodiacRoasts = cfg.copy.zodiac[profile.zodiac]?.roasts ?? []
  const tenureRoasts = cfg.copy.tenure[profile.tenure] ?? []
  const roastTexts = [
    ...pickText(zodiacRoasts, rng, 1),
    ...pickText(tenureRoasts, rng, 1)
  ]

  const deptJokes = cfg.copy.department[profile.dept] ?? []
  const deptJoke = pickOne(rng, deptJokes) ?? ''

  const luckyPool = cfg.copy.lucky
  const lucky = {
    color: pickOne(rng, luckyPool.colors) ?? '',
    number: pickOne(rng, luckyPool.numbers) ?? 0,
    direction: pickOne(rng, luckyPool.directions) ?? '',
    zodiac: pickOne(rng, luckyPool.zodiacs) ?? ''
  }

  return {
    scores,
    bands,
    avg,
    identity: drawIdentity(profile, cfg.copy.identity, rng),
    roastTexts,
    deptJoke,
    fortune: drawFortune(avg, cfg.copy.fortunes, rng),
    lucky,
    vars
  }
}
```

`buildReading` 是唯一产出结果的地方，**每次占卜只调用一次**。分数、签文、标签、彩蛋全部在同一次调用里定下来，之后只读。

- [ ] **Step 4: 运行测试确认通过**

```bash
npx vitest run src/core/fortune.test.js
```

Expected: PASS

- [ ] **Step 5: 运行全部测试**

```bash
npx vitest run
```

Expected: 全部 PASS

- [ ] **Step 6: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 14: 文案合规扫描 `lint-copy.js`

**Files:**
- Create: `scripts/lint-copy.js`
- Modify: `package.json`（把 `build` 脚本改成 lint 前置）
- Test: `scripts/lint-copy.test.js`

**Interfaces:**
- Consumes: `src/config/copy/*.json`、`src/config/settings.json`、`src/config/lint-rules.json`（Task 12 创建，合规规则单一来源）、`src/**/*.vue`
- Produces: 命令行脚本，**退出码 0 = 通过、1 = 命中以下几种情况之一**：
  - 文案或配置中命中禁用词
  - **`.vue` 模板中命中禁用词**（匹配前会去掉全部空白，故 `占 卜` 这类字间距逃逸也会被捕获）
  - **存在未填写的占位符**（`【待填写…`）—— 这一条**从警告升级为中止**，理由见 Step 5
- **不要再写「0 但打印警告」**：占位符已不再是警告。这一行的早期版本与 Step 5 矛盾过。

- [ ] **Step 1: 写失败测试**

```js
// scripts/lint-copy.test.js
import { describe, it, expect } from 'vitest'
import { readFileSync, mkdtempSync, mkdirSync, writeFileSync, rmSync } from 'node:fs'
import { join, dirname, resolve } from 'node:path'
import { tmpdir } from 'node:os'
import { execFileSync } from 'node:child_process'
import { fileURLToPath } from 'node:url'
import { lintText, lintObject, findPlaceholders, lintTemplate } from './lint-copy.js'

const ROOT = join(dirname(fileURLToPath(import.meta.url)), '..')

describe('lintText', () => {
  it('干净文案返回空数组', () => {
    expect(lintText('今天宜早退，忌开会。')).toEqual([])
  })

  it('命中负面预言词', () => {
    expect(lintText('小心破财').length).toBe(1)
    expect(lintText('当心被裁').length).toBe(1)
  })

  it('命中迷信话术词', () => {
    expect(lintText('今日占卜结果').length).toBe(1)
    expect(lintText('可以改运').length).toBe(1)
  })

  it('命中去空白后才显形的禁用词（JSON 路径同样必须压缩空白）', () => {
    // 这是本任务最重要的回归保护之一。字间距在本项目里是通用约定
    // （`填 4 个空，摇一支签`、`解 锁`），不只在模板里 —— 只压缩模板路径，
    // 会让承载绝大多数用户可见文案的 JSON 路径留一条逃逸通道。
    expect(lintText('占 卜').length).toBe(1)
    expect(lintText('今 日 破 财').length).toBe(1)
    expect(lintText('开 始 占 卜').length).toBe(1)
  })

  it('长度按原始文本计，不因压缩空白而放宽', () => {
    // 60 个字符里含空格时仍算 60 字 —— 空格确实占版面
    const withSpaces = '字 '.repeat(30) + '字' // 61 个字符
    expect(lintText(withSpaces).some((m) => m.includes('60'))).toBe(true)
  })

  it('超过 60 字报错', () => {
    const long = '字'.repeat(61)
    expect(lintText(long).some((m) => m.includes('60'))).toBe(true)
  })

  it('恰好 60 字不报错', () => {
    expect(lintText('字'.repeat(60))).toEqual([])
  })

  it('非字符串输入安全跳过', () => {
    expect(lintText(123)).toEqual([])
    expect(lintText(null)).toEqual([])
  })
})

describe('lintObject', () => {
  it('递归检查嵌套对象与数组，并给出路径', () => {
    const obj = { a: { b: ['干净', '这里破财了'] } }
    const issues = lintObject(obj, 'root')
    expect(issues).toHaveLength(1)
    expect(issues[0].path).toContain('root.a.b[1]')
  })

  it('全部干净时返回空数组', () => {
    expect(lintObject({ a: ['好的', '也好'] }, 'root')).toEqual([])
  })
})

describe('findPlaceholders', () => {
  it('找出含【待填写 的字符串', () => {
    expect(findPlaceholders({ a: '【待填写：项目名】' }, 'root')).toHaveLength(1)
  })

  it('无占位符返回空', () => {
    expect(findPlaceholders({ a: '正常文案' }, 'root')).toEqual([])
  })
})

describe('lintTemplate', () => {
  it('捕获被字间距拆开的禁用词', () => {
    // 这是子串式合规检查的经典逃逸路径：模板用字面空格实现字间距，
    // 源码里不含连续子串「占卜」，但用户读到的就是「开始占卜」。
    const raw = '<template><button>开 始 占 卜</button></template>'
    expect(lintTemplate(raw).length).toBe(1)
    expect(lintTemplate(raw)[0].message).toContain('占卜')
  })

  it('捕获未加空格的禁用词', () => {
    expect(lintTemplate('<template><p>今日占卜结果</p></template>').length).toBe(1)
  })

  it('正常模板文案不报错', () => {
    expect(lintTemplate('<template><button>开 始 摇 签</button></template>')).toEqual([])
    // 注意：这里**不**断言「工位风水」被豁免 —— 它本来就不含禁用词，
    // 断言它会永远通过，属于假验证（此前确有此问题）。
    expect(lintTemplate('<template><p>工位风水研究员</p></template>')).toEqual([])
  })

  it('没有 template 块时返回空数组', () => {
    expect(lintTemplate('<script>export default {}</script>')).toEqual([])
  })

  it('路径会带进问题描述', () => {
    const issues = lintTemplate('<template><p>占卜</p></template>', 'src/views/X.vue')
    expect(issues[0].path).toBe('src/views/X.vue')
  })
})

describe('构建接线与闸门阻断（端到端）', () => {
  it('package.json 的 build 脚本以 && 串联 linter 与打包', () => {
    // 这是整套合规机制的**单点**：若 build 不再调用 linter，所有检查都会
    // 静默失效，而本文件其余测试**一条都不会红** —— 它们测的是纯函数，
    // 不是「闸门是否接在构建上」。这一条就是为那个单点立的。
    //
    // 变异验证：把 package.json 的 build 改回 `"vite build"`，本测试必须失败。
    const pkg = JSON.parse(readFileSync(join(ROOT, 'package.json'), 'utf8'))
    expect(pkg.scripts.build).toMatch(/lint-copy\.js\s*&&\s*vite build/)
  })

  it('四种情形各自的退出码正确（真跑子进程，不靠断言源码文本）', () => {
    // 为什么要真跑：`lintText` 返回问题数组 ≠ 进程以非零码退出，
    // 而「违规时阻断构建」正是本脚本存在的全部意义。
    //
    // 为什么不用源码文本断言：先前试过「数 process.exit(1) 出现次数 >= 2」，
    // 但文件里有 **3** 处 exit，把违规分支改成 exit(0) 后仍剩 2 处、照样通过 ——
    // 计数下限无法指出是哪一条分支被改。而带字符偏移的「有界窗口」断言会随
    // 无关格式调整误报。两者都不如直接跑一次。
    const tmp = mkdtempSync(join(tmpdir(), 'lint-copy-'))
    const run = () => {
      try {
        execFileSync(process.execPath, [join(ROOT, 'scripts', 'lint-copy.js'), `--root=${tmp}`], { stdio: 'pipe' })
        return 0
      } catch (e) {
        return e.status ?? -1
      }
    }
    const writeSettings = (donationText) =>
      writeFileSync(join(tmp, 'src/config/settings.json'), JSON.stringify({ passcode: 'x', donationText }))

    try {
      mkdirSync(join(tmp, 'src/config/copy'), { recursive: true })
      mkdirSync(join(tmp, 'src/views'), { recursive: true })
      writeFileSync(
        join(tmp, 'src/config/lint-rules.json'),
        JSON.stringify({ maxLength: 60, banned: ['破财', '占卜'], placeholderMark: '【待填写' })
      )
      // ⚠️ 必须放**至少一个干净的文案文件**。只造空目录会让 linter 的
      // 「语料为空即失败」守卫先触发，于是：
      //   ① 「干净 → 退出 0」永远不可能通过；
      //   ② 后面几条「非零」会**因为错误的原因**通过（空语料下怎么跑都非零），
      //      从而掩盖真实回归。
      // 夹具必须与真实仓库同构，否则它测的是夹具自己的退化行为。
      writeFileSync(join(tmp, 'src/config/copy/ui.json'), JSON.stringify({ a: '正常文案' }))

      // ① 干净 → 退出 0
      writeSettings('本次筹款将捐至儿童助学项目')
      expect(run()).toBe(0)

      // ② 存在未填占位符 → 非零（这正是从警告升级为阻断的那个行为）
      writeSettings('【待填写：公益项目】')
      expect(run()).not.toBe(0)

      // ③ JSON 文案命中禁用词 → 非零
      writeSettings('本次筹款将捐至儿童助学项目')
      writeFileSync(join(tmp, 'src/config/copy/ui.json'), JSON.stringify({ a: '今天宜破财' }))
      expect(run()).not.toBe(0)

      // ④ 模板里命中被字间距拆开的禁用词 → 非零
      writeFileSync(join(tmp, 'src/config/copy/ui.json'), JSON.stringify({ a: '正常文案' }))
      writeFileSync(join(tmp, 'src/views/X.vue'), '<template><button>开 始 占 卜</button></template>')
      expect(run()).not.toBe(0)
    } finally {
      rmSync(tmp, { recursive: true, force: true })
    }
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npx vitest run scripts/lint-copy.test.js
```

Expected: FAIL —— `Failed to resolve import "./lint-copy.js"`

- [ ] **Step 3: 实现 `scripts/lint-copy.js`**

```js
// scripts/lint-copy.js
// 文案合规扫描。挂载为 npm run build 前置：命中红线即构建失败。
import { readFileSync, readdirSync } from 'node:fs'
import { join, dirname, basename, relative, resolve } from 'node:path'
import { fileURLToPath, pathToFileURL } from 'node:url'

const __dirname = dirname(fileURLToPath(import.meta.url))
// 项目根可用 `--root=<dir>` 覆盖，**唯一的用途是让测试能在临时目录里跑真实 CLI**。
//
// 为什么需要它：本脚本存在的全部意义是「违规时阻断构建」，而这一点
// **纯函数测试覆盖不到** —— `lintText` 返回问题数组，但「返回了问题数组」
// 与「进程真的以非零码退出」是两回事。没有这个开关时，只能去断言源码文本
// （例如数 process.exit 出现次数），那种断言既会随无关格式调整而误报，
// 又无法指出是哪条分支 —— 两头不讨好。
const rootArg = process.argv.find((a) => a.startsWith('--root='))
const ROOT = rootArg ? resolve(rootArg.slice('--root='.length)) : join(__dirname, '..')
const COPY_DIR = join(ROOT, 'src/config/copy')
const SETTINGS = join(ROOT, 'src/config/settings.json')
const RULES_FILE = join(ROOT, 'src/config/lint-rules.json')
const SRC_DIR = join(ROOT, 'src')

// 合规规则的单一来源。src/core/copyLibrary.test.js 读同一份文件 ——
// 禁用词表若在两处各写一遍，迟早会漂移成两套标准。
const RULES = JSON.parse(readFileSync(RULES_FILE, 'utf8'))
const MAX_LEN = RULES.maxLength
const BANNED = RULES.banned
const PLACEHOLDER_MARK = RULES.placeholderMark

/**
 * 去掉全部空白。
 *
 * ⚠️ **两条路径都必须用它**：JSON 文案与 `.vue` 模板。
 * 字间距在本项目里是通用约定（`填 4 个空，摇一支签`、`解 锁`、`开 始`），
 * 不只出现在模板里 —— 只在模板路径压缩，等于给承载绝大多数用户可见文案的
 * JSON 路径留了一条逃逸通道：`"占 卜"` 会绕过全部检查，而用户读到的就是「占卜」。
 */
const SQUEEZE = (s) => s.replace(/\s+/g, '')

/** 检查单条文本，返回问题描述数组 */
export function lintText(text) {
  if (typeof text !== 'string') return []
  const issues = []

  // 长度按**原始**文本计：空格确实占版面，不该被压缩掉
  if ([...text].length > MAX_LEN) {
    issues.push(`超过 ${MAX_LEN} 字（${[...text].length} 字）：${text.slice(0, 20)}…`)
  }

  // 禁用词按**去空白后**的文本匹配
  const squeezed = SQUEEZE(text)
  for (const word of BANNED) {
    if (squeezed.includes(word)) {
      issues.push(`命中禁用词「${word}」：${text}`)
    }
  }

  return issues
}

/** 递归检查任意 JSON 结构，返回 { path, message } 数组 */
export function lintObject(node, path = 'root', out = []) {
  if (typeof node === 'string') {
    for (const msg of lintText(node)) out.push({ path, message: msg })
  } else if (Array.isArray(node)) {
    node.forEach((child, i) => lintObject(child, `${path}[${i}]`, out))
  } else if (node && typeof node === 'object') {
    for (const [key, child] of Object.entries(node)) {
      lintObject(child, `${path}.${key}`, out)
    }
  }
  return out
}

/** 查找未替换的占位符 */
export function findPlaceholders(node, path = 'root', out = []) {
  if (typeof node === 'string') {
    if (node.includes(PLACEHOLDER_MARK)) out.push({ path, message: node })
  } else if (Array.isArray(node)) {
    node.forEach((child, i) => findPlaceholders(child, `${path}[${i}]`, out))
  } else if (node && typeof node === 'object') {
    for (const [key, child] of Object.entries(node)) {
      findPlaceholders(child, `${path}.${key}`, out)
    }
  }
  return out
}

function readJson(path) {
  return JSON.parse(readFileSync(path, 'utf8'))
}

/** 递归收集 src 下所有 .vue 文件 */
function collectVueFiles(dir, out = []) {
  for (const entry of readdirSync(dir, { withFileTypes: true })) {
    const p = join(dir, entry.name)
    if (entry.isDirectory()) collectVueFiles(p, out)
    else if (entry.name.endsWith('.vue')) out.push(p)
  }
  return out
}

/**
 * 检查 .vue 模板里的硬编码文案。返回问题描述数组。
 *
 * 为什么需要单独一遍扫描：
 *  1. **用户的可见文案不只在 `config/copy/*.json` 里。** 按钮、标题、表单标签
 *     大量硬编码在模板中，它们从不经过 JSON 扫描 —— 只查 copy 目录会漏掉一半。
 *  2. **必须先去掉所有空白再匹配。** 模板里的字间距常用字面空格实现，
 *     例如 `开 始 占 卜` 在源码里**不含**连续子串 `占卜`，逐字子串匹配会全部放行，
 *     而用户读到的就是「开始占卜」。这是子串式合规检查的经典逃逸路径。
 */
export function lintTemplate(raw, path = 'template') {
  const m = String(raw).match(/<template>([\s\S]*)<\/template>/)
  if (!m) return []
  const squeezed = SQUEEZE(m[1])
  const issues = []
  for (const word of BANNED) {
    if (squeezed.includes(word)) {
      issues.push({ path, message: `模板文案命中禁用词「${word}」（已忽略字间距）` })
    }
  }
  return issues
}

function main() {
  const files = readdirSync(COPY_DIR).filter((f) => f.endsWith('.json'))
  if (files.length === 0) {
    console.error('✗ src/config/copy/ 下没有找到任何 json 文件')
    process.exit(1)
  }

  const violations = []
  const placeholders = []

  for (const file of files) {
    const path = join(COPY_DIR, file)
    let data
    try {
      data = readJson(path)
    } catch (e) {
      violations.push({ path: file, message: `JSON 解析失败：${e.message}` })
      continue
    }
    const name = basename(file, '.json')
    for (const issue of lintObject(data, name)) violations.push(issue)
    for (const ph of findPlaceholders(data, name)) placeholders.push(ph)
  }

  const settings = readJson(SETTINGS)
  for (const issue of lintObject(settings, 'settings')) violations.push(issue)
  for (const ph of findPlaceholders(settings, 'settings')) placeholders.push(ph)

  // 模板里的硬编码用户文案（按钮、标题、表单标签）从不经过 JSON 扫描，
  // 必须单独过一遍；且匹配前会去掉全部空白，防止字间距成为逃逸通道。
  const vueFiles = collectVueFiles(SRC_DIR)
  for (const path of vueFiles) {
    for (const issue of lintTemplate(readFileSync(path, 'utf8'), relative(ROOT, path))) {
      violations.push(issue)
    }
  }

  if (violations.length > 0) {
    console.error('\n✗ 文案合规检查未通过：\n')
    for (const v of violations) console.error(`  ${v.path}\n    ${v.message}\n`)
    console.error(`共 ${violations.length} 处问题。构建已中止。\n`)
    process.exit(1)
  }

  if (placeholders.length > 0) {
    console.error('\n✗ 检测到未替换的占位符，构建已中止。\n')
    console.error('  占位符是给运营看的内部批注，绝不能进入交付物。')
    console.error('  填入真实值后重新构建即可通过。\n')
    for (const p of placeholders) console.error(`  ${p.path}  →  ${p.message}`)
    console.error('')
    process.exit(1)
  }

  console.log(`✓ 文案合规检查通过（${files.length} 个文案文件 + ${vueFiles.length} 个模板，无占位符残留）`)
}

// 仅在被直接执行时运行 main，被 import 时不执行
if (process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href) {
  main()
}
```

末位的 `import.meta.url` 判断让这个文件既能当 CLI 跑，又能被测试 import 而不触发 `process.exit`。

- [ ] **Step 4: 运行测试确认通过**

```bash
npx vitest run scripts/lint-copy.test.js
```

Expected: PASS

- [ ] **Step 5: 跑一次真实扫描，确认占位符闸门生效**

```bash
node scripts/lint-copy.js; echo "exit=$?"
```

Expected: 打印 `✗ 检测到未替换的占位符，构建已中止。`，列出 `settings.donationText`、`settings.eventName`、`settings.organizer` 三处，`exit=1`。

**这是预期行为，不是 bug。** 此刻 `settings.json` 确实还没填真实值 —— 闸门正确地拦住了它。这正是本步骤要看到的：闸门是**阻断式**的，不是警告式的。

**为什么必须阻断而不能只警告**：`IntroView` 已用 `isPlaceholder()` 把占位符挡在渲染之外，所以漏填不会在页面上显形 —— 它会**静默**地让开场页缺少公益捐款说明（PRD 8.6 要求明示）。一个每次构建都出现、却从不阻止任何事的警告，会迅速变成背景噪音；而静默的合规缺失没人会发现。把闸门做成阻断式，是让这件事在唯一必然发生的时刻（构建）以不可忽略的方式暴露。

- [ ] **Step 6: 把 linter 挂成 build 前置**

编辑 `package.json`，把 `scripts.build` 从 `"vite build"` 改为：

```json
"build": "node scripts/lint-copy.js && vite build",
```

`&&` 让 lint 不通过时构建直接中止 —— 这条是本任务的核心价值：PRD 第八章的合规红线从「文档里的要求」变成「构建时的硬约束」。

- [ ] **Step 7: 验证闸门的两条路径（先失败，再通过）**

先确认失败路径：

```bash
npm run build; echo "exit=$?"
```

Expected: lint 阶段因占位符中止，**不产出新的 `dist/`**，`exit=1`。

再确认通过路径 —— 临时给 `settings.json` 填入真实值：

```json
{
  "passcode": "搬砖",
  "displayTitle": "职场运势互动",
  "donationText": "本次筹款将捐至儿童助学项目",
  "eventName": "公益市集",
  "organizer": "XX 公司工会",
  "posterBaseUrl": "",
  "enableCount": true
}
```

```bash
npm run build; echo "exit=$?"
```

Expected: lint 通过，`vite build` 成功，产出 `dist/`，`exit=0`。

**验证完把 `donationText` / `eventName` / `organizer` 改回占位符**，再跑一次确认闸门重新拦住。**必须改回** —— 后续任务依赖占位符仍可被检出，而真实值应由用户在上线前决定，不由代码库预设。

- [ ] **Step 8: 验证 lint 能拦住违规文案**

临时在 `src/config/copy/tenure.json` 的某条文案里插入「破财」，运行：

```bash
node scripts/lint-copy.js; echo "exit=$?"
```

Expected: 打印命中的文件路径与禁用词，`exit=1`。

**验证完把「破财」删掉，恢复原文案。**

- [ ] **Step 9: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

**注意：本任务之后 `npm run build` 会因占位符而持续失败，直到 `settings.json` 填入真实值。** 这是设计。后续任务做产物体积或零上报核验时，用 `npx vite build` 绕过 lint 前置；而 Task 18 的上线闸门会再次走 `node scripts/lint-copy.js`。

---

## Task 15: 摇签筒动画

**Files:**
- Create: `src/styles/animation.css`
- Modify: `src/views/DivinationView.vue`

**Interfaces:**
- Consumes: `fortune.js` 的 `buildReading`、`rng.js` 的 `makeRng`、`state.js` 的 `setReading` / `store.profile`、`config.js` 的 `runtimeConfig`、`storage.js` 的 `bumpCount`
- Produces: `DivinationView` emit `done`，并在 emit 前完成 `setReading(reading)`

- [ ] **Step 1: 创建 `src/styles/animation.css`**

```css
.tube-stage {
  position: relative;
  width: 180px;
  height: 220px;
  margin: 0 auto;
}

.tube {
  position: absolute;
  bottom: 0;
  left: 50%;
  width: 96px;
  height: 150px;
  margin-left: -48px;
  border-radius: 10px 10px 14px 14px;
  background: linear-gradient(180deg, #6b3f2a 0%, #4a2a1c 60%, #352014 100%);
  border: 2px solid var(--gold-dim);
  box-shadow: inset 0 0 22px rgba(0, 0, 0, 0.55);
}

.tube::after {
  content: '';
  position: absolute;
  inset: 8px;
  border: 1px solid rgba(232, 192, 116, 0.35);
  border-radius: 6px;
}

.tube--shaking {
  animation: tube-shake 0.42s ease-in-out infinite;
}

@keyframes tube-shake {
  0%, 100% { transform: rotate(-7deg); }
  50%      { transform: rotate(7deg); }
}

.stick {
  position: absolute;
  bottom: 120px;
  left: 50%;
  width: 26px;
  height: 128px;
  margin-left: -13px;
  border-radius: 6px 6px 3px 3px;
  background: linear-gradient(180deg, #f3e2bd 0%, #e2c793 55%, #c9a86a 100%);
  border: 1px solid var(--gold-dim);
  color: #6b4a12;
  font-size: 15px;
  font-weight: 700;
  writing-mode: vertical-rl;
  text-align: center;
  padding-top: 12px;
  opacity: 0;
}

.stick--drawn {
  animation: stick-draw 1.05s cubic-bezier(0.22, 1.1, 0.36, 1) forwards;
}

@keyframes stick-draw {
  0%   { opacity: 0; transform: translateY(96px) scale(0.85); }
  45%  { opacity: 1; transform: translateY(-22px) scale(1.06); }
  70%  { transform: translateY(-8px) scale(1); }
  100% { opacity: 1; transform: translateY(-14px) scale(1.14); }
}

.glow {
  position: absolute;
  inset: -60px;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(232, 192, 116, 0.42) 0%, rgba(232, 192, 116, 0) 68%);
  opacity: 0;
  pointer-events: none;
}

.glow--on {
  animation: glow-pulse 0.9s ease-out forwards;
}

@keyframes glow-pulse {
  0%   { opacity: 0; transform: scale(0.7); }
  50%  { opacity: 1; transform: scale(1); }
  100% { opacity: 0.25; transform: scale(1.15); }
}

.starfield {
  position: absolute;
  inset: -40px;
  background-image:
    radial-gradient(1.5px 1.5px at 18% 22%, rgba(255, 255, 255, 0.75), transparent),
    radial-gradient(1.5px 1.5px at 72% 14%, rgba(255, 255, 255, 0.6), transparent),
    radial-gradient(1.5px 1.5px at 38% 68%, rgba(232, 192, 116, 0.75), transparent),
    radial-gradient(1.5px 1.5px at 86% 58%, rgba(255, 255, 255, 0.5), transparent),
    radial-gradient(1.5px 1.5px at 10% 84%, rgba(232, 192, 116, 0.6), transparent),
    radial-gradient(1.5px 1.5px at 58% 92%, rgba(255, 255, 255, 0.45), transparent);
  animation: twinkle 2.4s ease-in-out infinite;
}

@keyframes twinkle {
  0%, 100% { opacity: 0.45; }
  50%      { opacity: 1; }
}

@media (prefers-reduced-motion: reduce) {
  .tube--shaking,
  .stick--drawn,
  .glow--on,
  .starfield { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; }
}
```

末位的 `prefers-reduced-motion` 是可访问性要求：前庭功能障碍用户对持续摇晃动画敏感，系统开启"减弱动态效果"时直接跳到终态。

- [ ] **Step 2: 在 `src/main.js` 中引入动画样式**

```js
import { createApp } from 'vue'
import App from './App.vue'
import './styles/theme.css'
import './styles/animation.css'

createApp(App).mount('#app')
```

- [ ] **Step 3: 实现 `src/views/DivinationView.vue`**

```vue
<script setup>
import { ref, onMounted, onBeforeUnmount } from 'vue'
import { runtimeConfig } from '../core/config.js'
import { makeRng } from '../core/rng.js'
import { buildReading } from '../core/fortune.js'
import { store, setReading, setStage } from '../core/state.js'
import { bumpCount } from '../core/storage.js'

const emit = defineEmits(['done'])
const ui = runtimeConfig.copy.ui.divining

// 总时长约 3s：摇签 1.5s → 弹出 1.05s → 定格 0.45s
const SHAKE_MS = 1500
const DRAW_MS = 1050

const shaking = ref(true)
const drawn = ref(false)
const glowing = ref(false)
const stepIndex = ref(0)

let timers = []

function later(fn, ms) {
  timers.push(setTimeout(fn, ms))
}

onMounted(() => {
  // 守卫：没有 profile 就无从计算（例如会话被清空后又落到本阶段）。
  // 不守卫的话 buildReading 里的 profile.name 会直接抛异常。
  if (!store.profile) {
    setStage('form')
    return
  }

  // 结果在此刻一次性算定并冻结，与动画抽出的签是同一支
  const profile = store.profile
  const seed = (Date.now() ^ Math.floor(Math.random() * 0xffffffff)) >>> 0
  const reading = buildReading(profile, runtimeConfig, makeRng(seed))
  setReading({ ...reading, seed })

  if (runtimeConfig.settings.enableCount) bumpCount()

  later(() => {
    shaking.value = false
    drawn.value = true
    glowing.value = true
    stepIndex.value = 1
  }, SHAKE_MS)

  later(() => {
    stepIndex.value = 2
  }, SHAKE_MS + DRAW_MS * 0.7)

  later(() => emit('done'), SHAKE_MS + DRAW_MS + 450)
})

onBeforeUnmount(() => {
  timers.forEach(clearTimeout)
  timers = []
})
</script>

<template>
  <section class="divining">
    <p class="divining__title">{{ ui.title }}</p>

    <div class="tube-stage">
      <div class="starfield" />
      <div class="glow" :class="{ 'glow--on': glowing }" />

      <div class="tube" :class="{ 'tube--shaking': shaking }" />

      <div v-if="drawn" class="stick stick--drawn">
        {{ store.reading?.fortune?.level ?? '' }}
      </div>
    </div>

    <ul class="steps">
      <li
        v-for="(s, i) in ui.steps"
        :key="s"
        class="steps__item"
        :class="{ 'steps__item--on': i <= stepIndex }"
      >
        {{ s }}
      </li>
    </ul>
  </section>
</template>

<style scoped>
.divining {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  /* 70px 不是随手定的：实测（getBoundingClientRect + 逐字符墨迹统计）
     在 34px 下，弹出的签会盖住标题「正在为你摇签」的第 3、4 个字 ——
     「你」整字 0 像素、「为」只剩 6px 碎片，标题实际渲染成「正在 + 一块米色 + 摇签」。
     几何关系：定格态签顶边 = stage 顶边 − 50.96px，故 clearance = gap − 50.96；
     要 ≥16px 则 gap ≥ 66.96，取 70 留约 3px 抖动余量。
     ⚠️ 注意本值只保证**定格态**（用户注视的那 450ms）；飞行中途（45% 关键帧、
     因 cubic-bezier 的 y1=1.1 > 1 还有轻微过冲）最低约 15.8px。
     若要再缩小，必须重测中途帧，而不是只套用上面这个公式。 */
  gap: 70px;
}
.divining__title { margin: 0; color: var(--text-sub); letter-spacing: 0.28em; font-size: 14px; }
.steps { display: flex; gap: 18px; margin: 0; padding: 0; list-style: none; }
.steps__item { font-size: 12px; color: var(--text-dim); transition: color 0.3s ease; }
.steps__item--on { color: var(--gold); }
</style>
```

签文等级在动画中被写在签身上，**用户亲眼看到的就是结果页那支签**（spec 5.2）。

- [ ] **Step 4: 验证**

```bash
npm run dev
```

手动验证：
1. 填完表单提交 → 进入动画页，签筒摇晃约 1.5s
2. 一支签从筒中弹出，签身上写有吉凶等级
3. 底部三个步骤依次点亮
4. 约 3s 后自动进入结果页
5. 动画页**无法跳过、无跳过按钮**（PRD 7.1 要求）
6. 打开 DevTools 的 Rendering 面板勾选 `Emulate CSS prefers-reduced-motion: reduce`，重新走一遍 → 动画应直接跳到终态

- [ ] **Step 5: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 16: 结果页展示组件

**Files:**
- Create: `src/components/ScoreRadar.vue`, `src/components/ScoreBars.vue`, `src/components/FortuneSlip.vue`, `src/components/LuckyEggs.vue`

**Interfaces:**
- Consumes: `runtimeConfig`
- Produces:
  - `<ScoreRadar :scores :bands :labels />` — SVG 四维雷达图，`scores` 为 `Record<dim, number>`
  - `<ScoreBars :scores :bands :labels />` — 四行分数条
  - `<FortuneSlip :fortune :ui />` — 签文卡片
  - `<LuckyEggs :lucky :ui />` — 彩蛋网格

- [ ] **Step 1: 创建 `src/components/ScoreRadar.vue`**

```vue
<script setup>
import { computed } from 'vue'

const props = defineProps({
  scores: { type: Object, required: true },
  bands: { type: Object, required: true },
  labels: { type: Object, required: true },
  focused: { type: Boolean, default: false }
})

const SIZE = 240
const CENTER = SIZE / 2
const RADIUS = 82

const dims = computed(() => Object.keys(props.scores))

function pointOn(index, total, ratio) {
  const angle = (Math.PI * 2 * index) / total - Math.PI / 2
  return {
    x: CENTER + Math.cos(angle) * RADIUS * ratio,
    y: CENTER + Math.sin(angle) * RADIUS * ratio
  }
}

function polygon(ratio) {
  return dims.value
    .map((_, i) => {
      const p = pointOn(i, dims.value.length, ratio)
      return `${p.x.toFixed(1)},${p.y.toFixed(1)}`
    })
    .join(' ')
}

const gridRings = [0.25, 0.5, 0.75, 1]
const dataPolygon = computed(() =>
  dims.value
    .map((dim, i) => {
      const ratio = Math.max(0.04, (props.scores[dim] ?? 0) / 100)
      const p = pointOn(i, dims.value.length, ratio)
      return `${p.x.toFixed(1)},${p.y.toFixed(1)}`
    })
    .join(' ')
)

const axisPoints = computed(() =>
  dims.value.map((_, i) => pointOn(i, dims.value.length, 1))
)
const vertexPoints = computed(() =>
  dims.value.map((dim, i) => {
    const ratio = Math.max(0.04, (props.scores[dim] ?? 0) / 100)
    return pointOn(i, dims.value.length, ratio)
  })
)
const labelPoints = computed(() =>
  dims.value.map((_, i) => pointOn(i, dims.value.length, 1.26))
)
</script>

<template>
  <svg
    class="radar"
    :class="{ 'radar--blurred': focused }"
    :viewBox="`0 0 ${SIZE} ${SIZE}`"
    role="img"
    :aria-label="dims.map((d) => `${labels[d]} ${scores[d]} 分`).join('，')"
  >
    <polygon
      v-for="ring in gridRings"
      :key="ring"
      :points="polygon(ring)"
      fill="none"
      stroke="rgba(232,192,116,0.18)"
      stroke-width="1"
    />
    <line
      v-for="(p, i) in axisPoints"
      :key="`axis-${i}`"
      :x1="CENTER"
      :y1="CENTER"
      :x2="p.x"
      :y2="p.y"
      stroke="rgba(232,192,116,0.18)"
      stroke-width="1"
    />
    <polygon :points="dataPolygon" fill="rgba(232,192,116,0.28)" stroke="var(--gold)" stroke-width="2" />
    <circle
      v-for="(p, i) in vertexPoints"
      :key="`v-${i}`"
      :cx="p.x"
      :cy="p.y"
      r="3.5"
      fill="var(--gold-bright)"
    />
    <text
      v-for="(p, i) in labelPoints"
      :key="`l-${i}`"
      :x="p.x"
      :y="p.y"
      text-anchor="middle"
      dominant-baseline="middle"
      class="radar__label"
    >
      {{ labels[dims[i]] }}
    </text>
  </svg>
</template>

<style scoped>
.radar { width: 100%; max-width: 260px; height: auto; display: block; margin: 0 auto; }
.radar--blurred { filter: blur(5px); }
.radar__label { fill: var(--text-sub); font-size: 11px; }
</style>
```

- [ ] **Step 2: 创建 `src/components/ScoreBars.vue`**

注意：`ScoreBars` 的 `locked` prop 与 `ScoreRadar` 的 `focused` prop 在本计划中**不被使用**，它们是为计划二的「半截结果遮罩」预留的。本计划不要传这两个 prop。

```vue
<script setup>
import { computed } from 'vue'

const props = defineProps({
  scores: { type: Object, required: true },
  bands: { type: Object, required: true },
  labels: { type: Object, required: true },
  bandMeta: { type: Array, required: true },
  locked: { type: Boolean, default: false }
})

const rows = computed(() =>
  Object.keys(props.scores).map((dim) => {
    const bandKey = props.bands[dim]
    const meta = props.bandMeta.find((b) => b.key === bandKey) ?? {}
    return {
      dim,
      label: props.labels[dim],
      score: props.scores[dim],
      bandLabel: meta.label ?? '',
      emoji: meta.emoji ?? ''
    }
  })
)
</script>

<template>
  <ul class="bars">
    <li v-for="row in rows" :key="row.dim" class="bars__row">
      <span class="bars__name">{{ row.label }}</span>
      <span class="bars__track">
        <span class="bars__fill" :style="{ width: `${row.score}%` }" />
      </span>
      <span class="bars__val">
        <template v-if="locked">▓▓</template>
        <template v-else>{{ row.emoji }} {{ row.bandLabel }} {{ row.score }}</template>
      </span>
    </li>
  </ul>
</template>

<style scoped>
.bars { margin: 0; padding: 0; list-style: none; display: flex; flex-direction: column; gap: 10px; }
.bars__row { display: grid; grid-template-columns: 62px 1fr 84px; align-items: center; gap: 10px; }
.bars__name { font-size: 13px; color: var(--text-sub); }
.bars__track { height: 8px; border-radius: 999px; background: rgba(255, 255, 255, 0.08); overflow: hidden; }
.bars__fill { display: block; height: 100%; border-radius: 999px; background: linear-gradient(90deg, var(--gold-dim), var(--gold-bright)); }
.bars__val { font-size: 12px; color: var(--gold); text-align: right; }
</style>
```

- [ ] **Step 3: 创建 `src/components/FortuneSlip.vue`**

```vue
<script setup>
defineProps({
  fortune: { type: Object, required: true },
  ui: { type: Object, required: true }
})
</script>

<template>
  <div class="slip">
    <p class="slip__level gold-text">{{ fortune.level }}</p>
    <p class="slip__verse">{{ fortune.verse }}</p>
    <div class="slip__rows">
      <p><span class="slip__tag slip__tag--yi">{{ ui.yiLabel }}</span>{{ fortune.yi }}</p>
      <p><span class="slip__tag slip__tag--ji">{{ ui.jiLabel }}</span>{{ fortune.ji }}</p>
    </div>
    <p class="slip__explain">{{ fortune.explain }}</p>
  </div>
</template>

<style scoped>
.slip {
  padding: 20px 18px;
  border: 1px solid var(--card-line);
  border-radius: var(--radius);
  background: var(--card-bg);
  text-align: center;
}
.slip__level { margin: 0 0 12px; font-size: 22px; letter-spacing: 0.3em; }
.slip__verse { margin: 0 0 16px; font-size: 16px; line-height: 1.9; color: var(--text-main); letter-spacing: 0.06em; }
.slip__rows { display: flex; flex-direction: column; gap: 8px; margin-bottom: 14px; }
.slip__rows p { margin: 0; font-size: 14px; color: var(--text-sub); }
.slip__tag {
  display: inline-block;
  width: 22px;
  height: 22px;
  line-height: 22px;
  margin-right: 8px;
  border-radius: 6px;
  font-size: 12px;
  text-align: center;
}
.slip__tag--yi { background: rgba(122, 200, 140, 0.22); color: #9fe0b0; }
.slip__tag--ji { background: rgba(220, 130, 130, 0.2); color: #f0a5a5; }
.slip__explain { margin: 0; padding-top: 12px; border-top: 1px dashed var(--card-line); font-size: 13px; color: var(--text-dim); line-height: 1.7; }
</style>
```

- [ ] **Step 4: 创建 `src/components/LuckyEggs.vue`**

```vue
<script setup>
defineProps({
  lucky: { type: Object, required: true },
  ui: { type: Object, required: true }
})
</script>

<template>
  <ul class="eggs">
    <li class="eggs__item">
      <span class="eggs__k">{{ ui.luckyColor }}</span>
      <span class="eggs__v">{{ lucky.color }}</span>
    </li>
    <li class="eggs__item">
      <span class="eggs__k">{{ ui.luckyNumber }}</span>
      <span class="eggs__v">{{ lucky.number }}</span>
    </li>
    <li class="eggs__item">
      <span class="eggs__k">{{ ui.luckyDirection }}</span>
      <span class="eggs__v">{{ lucky.direction }}</span>
    </li>
    <li class="eggs__item">
      <span class="eggs__k">{{ ui.luckyZodiac }}</span>
      <span class="eggs__v">{{ lucky.zodiac }}</span>
    </li>
  </ul>
</template>

<style scoped>
.eggs {
  margin: 0;
  padding: 0;
  list-style: none;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}
.eggs__item {
  padding: 12px;
  border: 1px solid var(--card-line);
  border-radius: 12px;
  background: var(--card-bg);
  text-align: center;
}
.eggs__k { display: block; font-size: 11px; color: var(--text-dim); margin-bottom: 6px; }
.eggs__v { display: block; font-size: 15px; color: var(--gold); }
</style>
```

- [ ] **Step 5: 验证组件渲染**

临时把 `src/views/ResultView.vue` 替换为：

```vue
<script setup>
import { computed } from 'vue'
import { runtimeConfig } from '../core/config.js'
import { store } from '../core/state.js'
import ScoreRadar from '../components/ScoreRadar.vue'
import ScoreBars from '../components/ScoreBars.vue'
import FortuneSlip from '../components/FortuneSlip.vue'
import LuckyEggs from '../components/LuckyEggs.vue'

const r = computed(() => store.reading)
</script>

<template>
  <section v-if="r" style="padding: 24px 0; display: flex; flex-direction: column; gap: 18px;">
    <ScoreRadar :scores="r.scores" :bands="r.bands" :labels="runtimeConfig.scoring.dimensionLabels" />
    <ScoreBars
      :scores="r.scores"
      :bands="r.bands"
      :labels="runtimeConfig.scoring.dimensionLabels"
      :band-meta="runtimeConfig.scoring.bands"
    />
    <FortuneSlip :fortune="r.fortune" :ui="runtimeConfig.copy.ui.result" />
    <LuckyEggs :lucky="r.lucky" :ui="runtimeConfig.copy.ui.result" />
  </section>
</template>
```

```bash
npm run dev
```

手动验证：走完流程后结果页出现雷达图（四轴标签为摸鱼指数/升职运/人脉运/财运，数据多边形有形状）、四行分数条（带图标与等级）、签文卡片（档位 + 七言 + 宜忌 + 解）、四个彩蛋格子。

- [ ] **Step 6: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 17: 结果页组装

**Files:**
- Modify: `src/views/ResultView.vue`

**Interfaces:**
- Consumes: 全部展示组件、`runtimeConfig`、`store`、`setStage` / `resetAll`
- Produces: 完整结果页；「再算一次」按钮调用 `resetAll()` 回到开场页

- [ ] **Step 1: 实现 `src/views/ResultView.vue`**

```vue
<script setup>
import { computed } from 'vue'
import { runtimeConfig } from '../core/config.js'
import { store, resetAll } from '../core/state.js'
import ScoreRadar from '../components/ScoreRadar.vue'
import ScoreBars from '../components/ScoreBars.vue'
import FortuneSlip from '../components/FortuneSlip.vue'
import LuckyEggs from '../components/LuckyEggs.vue'
import Disclaimer from '../components/Disclaimer.vue'

const r = computed(() => store.reading)
const dimLabels = runtimeConfig.scoring.dimensionLabels
const bandMeta = runtimeConfig.scoring.bands
const ui = runtimeConfig.copy.ui.result

function again() {
  resetAll()
}
</script>

<template>
  <section v-if="r" class="result">
    <header class="result__head">
      <p class="result__who">{{ r.vars.name }} · {{ r.vars.dept }}</p>
      <p class="result__meta">{{ r.vars.zodiac }} · {{ r.vars.tenure }}</p>
    </header>

    <div class="card card--identity">
      <p class="card__k">{{ ui.identityLabel }}</p>
      <p class="card__v gold-text">{{ r.identity.text }}</p>
    </div>

    <div class="card">
      <p class="card__k">{{ ui.scoresLabel }}</p>
      <ScoreRadar :scores="r.scores" :bands="r.bands" :labels="dimLabels" />
      <ScoreBars :scores="r.scores" :bands="r.bands" :labels="dimLabels" :band-meta="bandMeta" />
    </div>

    <div class="card">
      <p class="card__k">{{ ui.roastLabel }}</p>
      <ul class="roasts">
        <li v-for="(t, i) in r.roastTexts" :key="i" class="roasts__item">{{ t }}</li>
      </ul>
    </div>

    <div class="card">
      <p class="card__k">{{ ui.deptLabel }}</p>
      <p class="card__body">{{ r.deptJoke }}</p>
    </div>

    <div class="card">
      <p class="card__k">{{ ui.fortuneLabel }}</p>
      <FortuneSlip :fortune="r.fortune" :ui="ui" />
    </div>

    <div class="card">
      <p class="card__k">{{ ui.luckyLabel }}</p>
      <LuckyEggs :lucky="r.lucky" :ui="ui" />
    </div>

    <button class="btn-gold result__again" @click="again">{{ ui.again }}</button>

    <Disclaimer />
  </section>
</template>

<style scoped>
.result {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 16px;
  padding: 26px 0 30px;
}
.result__head { text-align: center; }
.result__who { margin: 0 0 4px; font-size: 20px; color: var(--text-main); }
.result__meta { margin: 0; font-size: 12px; color: var(--text-dim); letter-spacing: 0.1em; }
.card {
  padding: 18px 16px;
  border: 1px solid var(--card-line);
  border-radius: var(--radius);
  background: var(--card-bg);
}
.card--identity { text-align: center; padding: 24px 16px; }
.card__k { margin: 0 0 14px; font-size: 12px; color: var(--text-dim); letter-spacing: 0.24em; text-align: center; }
.card__v { margin: 0; font-size: 26px; letter-spacing: 0.06em; }
.card__body { margin: 0; font-size: 15px; line-height: 1.9; color: var(--text-sub); text-align: center; }
.roasts { margin: 0; padding: 0; list-style: none; display: flex; flex-direction: column; gap: 12px; }
.roasts__item { font-size: 15px; line-height: 1.9; color: var(--text-sub); }
.roasts__item::before { content: '· '; color: var(--gold); }
.result__again { margin-top: 8px; align-self: center; }
</style>
```

- [ ] **Step 2: 端到端验证主链路**

```bash
npm run dev
```

完整走查：
1. 开场页 → 点开始
2. 填 4 个字段 → 提交
3. 摇签筒动画约 3s
4. 结果页出现全部 6 个模块（身份标签 / 四维雷达+分数条 / 吐槽 / 部门梗 / 签文 / 彩蛋）
5. 动画中签身的等级与结果页签文的 `level` **一致**
6. 点「再算一次」→ 回到开场页，重填同样输入 → 分数与签文**有变化**（随机波动生效）
7. 结果页向下滚动无橡皮筋回弹（iOS 真机）

- [ ] **Step 3: 运行全部测试与构建**

```bash
npm run test
npm run build
```

Expected: 测试全部 PASS；`vite build` 打包成功。

**这里用 `npx vite build` 而非 `npm run build`** —— 后者的 lint 前置会因 `settings.json` 仍有占位符而中止（Task 14 起即为设计如此）。本任务只关心打包是否成功。

- [ ] **Step 4: 任务收尾**

本项目不使用版本控制，无提交步骤。确认本任务的测试全部通过，即可进入下一任务。

---

## Task 18: 产物体积与零上报核验

**Files:**
- 无新增（核验任务）

**Interfaces:**
- Consumes: `dist/` 构建产物
- Produces: 一份核验结论

- [ ] **Step 1: 构建并统计产物体积**

```bash
npx vite build
```

**用 `npx vite build` 而非 `npm run build`** —— 后者会因占位符闸门而中止（见 Step 5）。本步骤只统计体积。

统计 `dist/` 下 js 与 css 的 gzip 体积：

```bash
find dist -type f \( -name "*.js" -o -name "*.css" \) -exec sh -c 'echo "$(gzip -c "$1" | wc -c) $1"' _ {} \;
```

Expected: JS gzip 合计 **< 80KB**，CSS gzip 合计 **< 12KB**，整体远低于 PRD 500KB 红线。若超了，检查是否误引入了非预期依赖（`npx vite build --report` 或查看 `dist/assets` 文件名与体积）。

- [ ] **Step 2: 本地起静态服务并核验零上报**

```bash
npx vite preview --port 4173
```

浏览器打开 `http://localhost:4173`，打开 DevTools 的 Network 面板，勾选 `Disable cache`，刷新页面，然后完整走一遍流程到结果页。

Expected: Network 面板中**除本站静态资源（html / js / css）外，没有任何第三方域名的请求**。不存在 analytics、埋点、字体 CDN、图片外链。

- [ ] **Step 3: 核验断网可用**

保持页面打开，在 DevTools 的 Network 面板把节流切换为 `Offline`，然后刷新页面。

Expected: 页面正常加载并可以完整走完流程（文案与逻辑全部内置，无网络依赖）。若刷新失败说明有未本地化的资源，需排查。

- [ ] **Step 4: 核验 localStorage / sessionStorage 内容**

在结果页的 Console 执行：

```js
JSON.stringify(Object.keys(localStorage))
JSON.stringify(Object.keys(sessionStorage))
```

Expected: `localStorage` 仅含 `["ld_count"]`；`sessionStorage` 仅含 `["ld_session"]`。**不存在任何第三方 key**。

- [ ] **Step 5: 上线闸门 —— 确认没有占位符残留**

`src/config/settings.json` 的 `passcode` / `donationText` / `eventName` / `organizer` 必须在部署前填成真实值。

```bash
node scripts/lint-copy.js; echo "exit=$?"
```

Expected: **`exit=0`**（lint 通过，说明无占位符残留）。

若 `exit=1` 且输出含「检测到未替换的占位符」，则本次核验结论为 **「不可上线」**，并把它作为最高优先级事项上报。

**匹配范围要比 `【待填写` 更宽** —— 代码里的 `isPlaceholder()` 只认这一个字面前缀，是人类手写变体（`[待填写：X]`、裸 `待填写：X`、`【待定】`）挡不住的。渲染守卫与上线闸门在这里**故意不一致**：守卫要精确（避免误伤 `【官方】` 这类正常文案），闸门要保守（宁可误报也不放过）。所以闸门走的是 linter 的 `【待填写` 检测**加上**下面这次人工确认：

```bash
grep -nE "待填写|待定|TODO|XXX|XX 公益" src/config/settings.json; echo "exit=$?"
```

Expected: 无输出（`exit=1`）。

**为什么这一步必须真的阻断**：`IntroView` 用 `isPlaceholder()` 把占位符挡在渲染之外，所以漏填**不会在页面上显形** —— 它会静默地让开场页缺少公益捐款说明（PRD 8.6 要求明示）。静默的合规缺失没人会发现，所以必须由这道闸门来兜。**若闸门报出残留却仍继续部署，等于这道防线不存在。**

逐个字段确认语义而非仅匹配字面：

| 字段 | 填成 | 影响 |
|---|---|---|
| `passcode` | 当日口令，如 `搬砖` | 解锁门槛；泄露即失效，每日一换 |
| `donationText` | 真实捐款去向 | 开场页展示；缺失则公益属性不明示 |
| `eventName` | 活动名称（**仅线下物料用，页面不渲染**） | 无 |
| `organizer` | 主办方 | 开场页展示 |

- [ ] **Step 6: 记录核验结论**

把核验结果追加到 `docs/superpowers/plans/2026-09-13-职场算命-计划一-核心引擎与主链路.md` 末尾的「执行记录」小节（若不存在则新建），内容包括：JS/CSS gzip 体积、Network 面板是否干净、断网是否可用、storage key 清单、**以及占位符闸门的结论**。本项目不使用版本控制，写完即可，无提交步骤。

---

## 执行完成后

计划一结束时，以下能力可用：

- 开场页、填表页、摇签筒动画、完整结果页
- 四维打分、等级带、身份标签、吐槽、部门梗、签文、彩蛋
- 会话恢复（刷新不丢表单与结果）
- 合规 linter 挂在 build 前置
- 全量文案库与单元测试

**尚不可用**（属计划二）：

- 生成分享海报与二维码
- 口令解锁半截结果
- 降级兜底路径

计划二在计划一通过验收后编写，因为它的接口依赖本计划的实际产物（`reading` 结构、`runtimeConfig` 形状、视图层约定）。

---

# 执行记录

## Task 18: 产物体积与零上报核验（2026-09-14）

核验任务，**无新增代码**，未修改 `src/` 与 `scripts/` 下任何文件。本节是核验结论的落地记录。

### 构建

用 **`npx vite build`**（**非** `npm run build` —— 后者前置 linter 会因占位符以 1 退出，那是闸门在正常工作）。vite v5.4.21，51 modules，546ms，退出码 0。产物哈希 `index-DTf_Kz5z.js` / `style-npZXV07C.css` 与核验前已存在的 `dist/` 一致，构建可复现。

### 1. 产物体积（`gzip -c <file> | wc -c`）

| 文件 | 原始字节 | gzip 字节 | gzip KiB |
|---|---:|---:|---:|
| `dist/assets/index-DTf_Kz5z.js` | 109 792 | **45 001** | 43.95 |
| `dist/assets/style-npZXV07C.css` | 9 888 | **2 806** | 2.74 |
| `dist/index.html` | 590 | **388** | 0.38 |
| **合计** | **120 270** | **48 195** | **47.06** |

- **JS gzip 合计 = 45 001 B = 43.95 KiB**，低于 brief 预期线 80 KB。
- **CSS gzip 合计 = 2 806 B = 2.74 KiB**，低于 brief 预期线 12 KB。
- 对照 PRD 500 KB 红线：全量原始 120 270 B = **23.5%**（按 500 KiB）/ 24.1%（按 500 000 B）；全量 gzip 48 195 B = **9.4%** / 9.6%。**远在预算之内。**
- `dist/` 只有 3 个文件，无 sourcemap（`sourceMappingURL` 出现 0 次），无图片/字体/wasm 等额外资源。
- 口径附注：vite 自身打印的 gzip 为 JS 45.70 kB / CSS 2.79 kB / HTML 0.38 kB，比 `gzip -c` 默认级别略大（压缩参数不同）。两个数字都对，量级一致，不影响结论。

### 2. 零上报核验（Network 面板）

`npx vite preview --port 4173`，清空 storage 后以 `ignoreCache` 刷新，完整走完 **开场页 → 填表 → 摇签动画 → 结果页**。全量请求（未按类型过滤）共 **4 条**：

| reqid | 请求 | 状态 |
|---|---|---|
| 5 | `GET http://localhost:4173/` | 200 |
| 6 | `GET http://localhost:4173/assets/index-DTf_Kz5z.js` | 200 |
| 7 | `GET http://localhost:4173/assets/style-npZXV07C.css` | 200 |
| 8 | `GET http://localhost:4173/favicon.ico` | 404 |

**完整请求 host 清单：`localhost:4173`，仅此一个。**

- 走完整个流程（含摇签动画、结果页四维分数、签文、彩蛋）**没有新增任何请求**。
- **无第三方域名**，无 analytics / 埋点 / 字体 CDN / 图片外链。
- 唯一一条非静态资源是同源 `favicon.ico` 404（浏览器默认行为，`index.html` 未声明 favicon）。**同源，不构成第三方上报**，但会在 Console 留一条 404 error。
- 静态复核与动态观测一致：CSS 中 `url(...)` / `@font-face` / `@import` 均为 **0 处**；源码中 `fetch(` / `XMLHttpRequest` / `WebSocket` / `EventSource` / `sendBeacon` / `axios` / `new Image` 均为 **0 处**。
- 澄清：bundle 中出现的 `http://www.w3.org/2000/svg`、`.../1998/Math/MathML`、`.../1999/xlink` 与 `https://vuejs.org/error-reference/#runtime-*` 是 **Vue 运行时的字符串字面量**（命名空间常量与报错文档链接），**不是请求目标** —— 实测中从未出现在 Network 面板里。判定为无害。

**结论：零上报成立。**

### 3. 断网可用性（**与 brief Step 3 预期不符，如实记录**）

两种情形结论相反，必须分开说：

**(a) 页面已驻留内存时 —— 完全可用。** 切到 `Offline` 后点击「再算一次」正常回到开场页，并重新完整走了一遍 填表 → 摇签 → 结果页，全程无报错，结果正常生成（另一次随机的分数与签文）。**证明整条主链路确实纯本地计算、无网络依赖。**

**(b) 断网状态下刷新页面 —— 失败。** 保持 `Offline` 执行 reload：导航以 `net::ERR_INTERNET_DISCONNECTED` 失败，页面变成 Chrome 恐龙错误页（`chrome-error://chromewebdata/`）。该错误页上后续的 `data:image/png;base64,...` 请求是 Chrome 内置错误页自身的资源，不是本应用的。

**根因（实测确认，非推测）：产物中没有任何 Service Worker。**

| 证据 | 实测值 |
|---|---|
| `dist/` 清单 | 只有 3 个文件，**无 `sw.js`** |
| 源码/产物中 `serviceWorker` / `registerSW` / `workbox` / `manifest` | **0 处** |
| `navigator.serviceWorker.getRegistrations()` | `swCount: 0` |
| `navigator.serviceWorker.controller` | `false` |
| `caches.keys()` | `[]` |

因此离线刷新只能依赖 HTTP 缓存；而刷新请求带 `cache-control: max-age=0` 强制回源校验（实测响应 `304`，走 ETag），离线时校验失败即整页失败。

**与 brief 的差异，明确记录不静默调和**：brief Step 3 的 Expected 是「页面正常加载并可以完整走完流程」，**未通过**。PRD 的表述是「加载后离线可用」，情形 (a) 满足该表述；brief 额外加严要求的是「刷新后仍可用」，情形 (b) 未通过。**风险真实存在**：活动现场网络抖动时，参会者一旦下拉刷新就会看到恐龙页而非应用，且没有任何提示或兜底。

**残余不确定性（不夸大结论）**：此处测的是 `vite preview` 的缓存头；阿里云静态托管若配置长缓存，刷新时浏览器可能直接从磁盘缓存出页面，表现或不相同。但**没有 Service Worker 就没有任何保障**，此点与托管商无关。故本条的稳健结论是「离线刷新无保障」，而非「在任何托管上都必然失败」。

### 4. storage key 清单

结果页 Console 实测：

```js
Object.keys(localStorage)   // ["ld_count"]
Object.keys(sessionStorage) // ["ld_session"]
```

```json
{"ls":["ld_count"],"ss":["ld_session"],"lsVals":{"ld_count":"1"}}
{"ls":["ld_count"],"ss":["ld_session"],"lsVals":{"ld_count":"2"},"ssLen":713}
```

- `localStorage` → **`["ld_count"]`**，值 `"1"`（一次流程后）/ `"2"`（两次后）
- `sessionStorage` → **`["ld_session"]`**，值 681 ~ 713 字节 JSON
- **与 brief 预期完全一致，无任何第三方 key。**
- 源码单一来源确认（全仓库无其他写入点）：`src/core/storage.js:3` `SESSION_KEY = 'ld_session'`；`src/core/storage.js:4` `COUNT_KEY = 'ld_count'`。
- 附带验证：写完后刷新页面**自动恢复到结果页**且签文分数不变（`ld_session` 生效），`ld_count` 保持 `"2"` **未被重复累加**（`restore()` 直接落结果页，未重跑 `DivinationView` 的计数钩子）。

### 5. 上线闸门结论

```bash
$ node scripts/lint-copy.js
✗ 检测到未替换的占位符，构建已中止。
  占位符是给运营看的内部批注，绝不能进入交付物。
  填入真实值后重新构建即可通过。
  settings.donationText  →  【待填写：本次筹款将捐至 XX 公益项目】
  settings.eventName  →  【待填写：活动名称】
  settings.organizer  →  【待填写：主办方】
LINT_EXIT=1

$ grep -n "【待填写" src/config/settings.json
4:  "donationText": "【待填写：本次筹款将捐至 XX 公益项目】",
5:  "eventName": "【待填写：活动名称】",
6:  "organizer": "【待填写：主办方】",
GREP_EXIT=0
```

linter 退出码 **1**，报 3 处占位符，与 grep 命中逐一对应。**闸门工作正常，`npm run build` 会被正确阻断。**

字段逐项核对：

| 字段 | 当前值 | 状态 | 影响 |
|---|---|---|---|
| `settings.passcode` | `搬砖` | **已填** | 不在闸门范围（闸门只报 3 处） |
| `settings.donationText` | `【待填写：本次筹款将捐至 XX 公益项目】` | **未填** | 开场页展示；缺失则公益属性不明示 |
| `settings.eventName` | `【待填写：活动名称】` | **未填** | 仅线下物料用，页面不渲染 |
| `settings.organizer` | `【待填写：主办方】` | **未填** | 开场页展示 |
| `settings.posterBaseUrl` | `""`（空串） | 空 | 属计划二海报功能，本次不涉及 |

> brief 的字段表要求 `passcode` 也须填成真实值；**实测它已被填为 `搬砖`**，故闸门只报 3 处而非 4 处。

### ⛔ 结论：**本次核验结论为「不可上线」**

**用户必须先填写 `donationText`、`eventName`、`organizer` 三个字段，再执行 `npm run build` 重新构建，才能部署。**

理由（沿用 brief Step 5 的判定，并已实测印证）：`IntroView` 用 `isPlaceholder()` 守卫了 `donationText` 与 `organizer`，占位符**不会**泄漏给参会者 —— **实测开场页确实没有渲染捐款说明区块**（`showDonation` / `showOrganizer` 均为 false）。但这意味着页面会**静默地**缺少公益捐款说明（PRD 8.6 要求明示）。**静默缺失比显式报错更危险**，所以必须由这道闸门兜底，而它确实兜住了。

**另需上报：当前 `dist/` 是旁路闸门构建出来的。** 本次按全局约束用 `npx vite build`，它不经过 linter；实测三串占位符文本**确实存在于 `dist/assets/index-DTf_Kz5z.js` 中**（仅因视图层守卫而未渲染，但仍在交付物里）。**用户填完字段后必须重新执行 `npm run build`，不可直接部署当前 `dist/`。** 这从反面证明了闸门的必要性：`npm run build` 会挡住，`npx vite build` 不会。

### 6. 核验汇总表

| 核验项 | 预期 | 实测 | 判定 |
|---|---|---|---|
| JS gzip | < 80 KB | 45 001 B (43.95 KiB) | 通过 |
| CSS gzip | < 12 KB | 2 806 B (2.74 KiB) | 通过 |
| 全量 vs PRD 500 KB | 远低于 | 120 270 B 原始 / 48 195 B gzip（23.5% / 9.4%） | 通过 |
| 请求 host 清单 | 仅本站 | `localhost:4173`（唯一） | 通过 |
| 第三方上报 | 无 | 无 | 通过 |
| 已加载页面离线走流程 | 可用 | 可用（完整走完一遍） | 通过 |
| **离线刷新** | brief 要求可用 | **`ERR_INTERNET_DISCONNECTED` 失败** | **未通过** |
| `localStorage` keys | `["ld_count"]` | `["ld_count"]` | 通过 |
| `sessionStorage` keys | `["ld_session"]` | `["ld_session"]` | 通过 |
| linter 退出码 | 1 | 1（3 处占位符） | 闸门正常 |
| 占位符残留 | 3 处 | 3 处 | **不可上线** |

### 7. 其他观察（超出本次核验范围）

- Console 一条 a11y issue：`No label associated with a form field (count: 1)`，对应「入职多久了」的 chip 按钮组无可关联 label。不影响功能与隐私承诺。
- `favicon.ico` 404：`index.html` 未声明 favicon。同源、不影响隐私；若想消除可加内联 SVG data URI favicon（保持零外链）。
- Console 无 `[config]` 警告：`checkConfigConsistency()` 返回空数组，6 个部门在三处配置齐备无漂移。

### 8. 遗留事项（交给用户）

1. **【最高优先级】** 填写 `settings.donationText`、`settings.eventName`、`settings.organizer` 的真实值。
2. 填完后执行 `npm run build`（**不是** `npx vite build`），确认 linter 输出 `✓ 文案合规检查通过` 后再部署新的 `dist/`。
3. **决策项**：是否为「离线刷新」补一个 Service Worker。若现场网络可承诺可靠，可接受现状；若不可靠，建议在计划二为 `dist/` 增加最小 SW（须与「零上报」承诺兼容 —— SW 只做本地缓存，不发任何请求）。

### 9. 环境与清理

- 构建 `npx vite build`（vite v5.4.21）退出码 0；服务 `npx vite preview --port 4173` 核验后**已终止**（PID 17308，已确认端口 4173 不再监听）。
- 核验用浏览器标签页已关闭；未新增任何临时文件；**未执行任何 `git` 命令**。
- 详细原始数据见 `.superpowers/sdd/plan-1-core-engine/task-18-report.md`。

### 10. 追加说明（终修波次，2026-09-14）

本节第 3 小节的 (b) 与第 6 小节的「离线刷新 → 未通过」，写的是**当时的实测事实**，保留不做修改。但其中「与 brief 预期不符」这一层矛盾**已经消解**，方式不是改实测记录，而是修正规格：

- **规格 §13.3 已改写**。原步骤把「DevTools 勾 Offline → **刷新** → 走完整流程」列为必做验收，比 PRD 第十三章的原文（「离线后**已加载页面**仍可正常算 + 生成海报」）更严。现改为「**完整加载页面** → 勾 Offline → **不刷新** → 走完整流程」，并明确：本步骤的通过标准是「已加载页面在断网下仍可走完流程」= 通过；「断网刷新后仍可用」**不在本次验收范围内**。
- **残余风险已在规格 §13.3 显式记录**：产物无 Service Worker，现场 WiFi 不稳时用户下拉刷新会卡在断网页。**加 Service Worker 属计划二范围，本次明确不做** —— 故第 8 小节遗留事项 3 的「决策项」到此裁决完毕（结论：不加）。
- 因此本记录与规格**不再互相矛盾**：记录说的是「断网刷新失败」（事实，未变），规格说的是「该情形不属于本次验收项」（范围，已改）。
- 同一波次还改了两处合规相关项，供后续读者对齐：① 禁用词表加入 `改命`（PRD 6.2 逐字点名）后为 **23 个**，规格 §11.1 与本计划 Global Constraints / Task 4 / Task 14 的清单已同步；② `scripts/lint-copy.js` 的扫描面从「`.vue` 的 `<template>` 块」扩展为「**整个 `.vue` 文件 + `src/` 下所有非测试 `.js`**」，匹配前先剥注释（否则 `rng.js` 注释里的「占卜」会误报）。第 5 小节引用的占位符闸门输出（3 处占位符、exit 1）在扩展后**仍然成立且已复测**。
- 本波次的完整结论与验证数据见 `.superpowers/sdd/plan-1-core-engine/final-fix-report.md`。
